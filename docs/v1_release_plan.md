# AxonVex v1.0 Release Plan — Deep Audit & Golden-MVP Roadmap

_Date: 2026-07-23 · Basis: full-tree review (core runtime, support subsystems, extension layers, docs/build/tests) + verified clean build with 369/369 tests passing. Complements `implementation_plan.md` and `project_backlog.md` with a release-focused sequencing._

---

## 1. Verdict in one paragraph

AxonVex has a genuinely solid skeleton — clean build (zero warnings), 369 passing tests, coherent library layering, wired CMake export, honest SRS/backlog — wrapped in a README that oversells it and a core with roughly a dozen real correctness bugs. The scheduler at the heart of the "real-time" claim executes **one task per cycle while holding a global mutex**, the safety e-stop is **not connected to anything**, and the "lock-free" memory pool has a **textbook ABA bug**. None of this is fatal: the fixes are well-scoped and the deletable bloat is identified. v1.0 scope is the **full project vision** — core runtime, visualization platform, enterprise security, hardware watchdog, MAVLink, cross-platform, distributed scale — which puts a defensible v1.0 at roughly 7–9 months solo (≈4–5 months with a second developer on the dashboard track), sequenced through alpha/beta/RC milestones below.

## 2. What is genuinely good (keep, build on)

- Build/test discipline: 0-warning build, 369 tests, CTest+GTest integration, 3-OS CI matrix, `.clang-format`.
- `ThreadSafeQueue` — correct Vyukov bounded MPMC queue.
- Trace record/replay (`traceReplay.*`) — small, correct, and strategically valuable (sells determinism).
- `AdapterBase` contract + contract tests; `interfaceUnits.hpp`; `MissionElement` abstraction; `errorHandler`; geometry/UUID types (one bug); `filesystem_compat.hpp`.
- CMake install/export scaffolding (`install(EXPORT axonvexTargets NAMESPACE axonvex::)`, Config + ConfigVersion files) — 80% of packaging exists.
- The SRS and backlog are honest and stratified; the marketing layer is the only dishonest document.

## 3. Critical defect inventory (fix before anything else)

Correctness bugs, each with location, verified by direct code reading:

| # | Severity | Defect | Location |
|---|---|---|---|
| C1 | CRITICAL | Scheduler executes only ONE task per cycle; runs user code while holding `tasksMutex_` (blocks addTask/removeTask/stats for the whole execution; N−1 ready tasks skipped per tick) | `timingController.cpp:277-317` |
| C2 | CRITICAL | SafetyManager never read after `setSafetyManager` — e-stop/policy violations do not halt the system | `system.cpp:497-506`, `system.hpp:715` |
| C3 | CRITICAL | MemoryPool lock-free free-list is ABA-vulnerable (no tag/version on head CAS); underpins thread-safe port writes | `memoryPool.hpp:118-142,181-182` |
| C4 | HIGH | `MemoryPool::isEmpty()`/`isFull()` bodies are swapped | `memoryPool.hpp:210-214` |
| C5 | HIGH | **FIXED.** `Configuration::applyUpdates` unlocked `config_mutex_` mid-merge — `lock.unlock(); notifyCallbacks(...); lock.lock();` — while the recursive merge lambda still held live `nlohmann::json` references into `config_data_`; a concurrent writer or the callback itself (e.g. `remove("a")`) could destroy the referenced node and the merge resumed writing through it (ASan: heap-use-after-free, `configuration.cpp:349`). Separately, `notifyCallbacks` invoked user callbacks while holding `callbacks_mutex_`, so a callback calling `registerCallback`/`unregisterCallback` self-deadlocked the non-recursive mutex — and since that mutex stayed locked forever, even `~Configuration()`'s `clearCallbacks()` hung on teardown. Fix: `applyUpdates` now merges fully under the lock into a local change list, then dispatches `notifyCallbacks` per change after releasing the lock; `notifyCallbacks` snapshots matching callbacks under `callbacks_mutex_` and invokes them unlocked, so a callback may freely call `set`/`remove`/`registerCallback`/`unregisterCallback`. Regression tests: `CallbackMayMutateConfigDuringUpdateNotification` (pre-fix ASan-aborts with heap-use-after-free), `CallbackMayReenterCallbackRegistration` (pre-fix deadlocks; deadline-guarded, worker detached), `ConcurrentUpdatesAndWritesAreRaceFree` (TSan guard for the fixed behavior). `loadFromJson` promoted from private to public (already fully implemented; the tests needed direct JSON-value loading). Verified: 404/404 ctest, ASan clean on `ConfigurationTest`, TSan clean on the three new tests (pre-existing `PerformanceBenchmarkTest` still fails its hardcoded ns-thresholds under TSan slowdown — same known class as C3's `MemoryPoolTest.PerformanceTest`, unrelated to C5). | `configuration.cpp` (fixed) |
| C6 | HIGH | **FIXED.** Declared-but-unimplemented public API: `saveConfiguration`, `exportDiagnostics`, `collectGarbage`, `registerRecoveryCallback` had zero definitions anywhere (link error if ever called). `initialize(const Configuration&)` was worse — it "succeeded" while silently ignoring its argument (logged a warning, forwarded to `initialize("")`, returned `true`), the banned silently-no-op-and-return-success shape. Verified zero callers in `src/`, `tests/`, `examples/` outside `system.hpp`/`system.cpp` themselves before deleting (project rule: implement and test in the same PR, or don't declare). **Breaking removal:** all five — `AxonVexSystem::saveConfiguration`, `::exportDiagnostics`, `::collectGarbage`, `::registerRecoveryCallback`, `::initialize(const Configuration&)` — no longer exist, along with the now-unused `recoveryCallbacks_` member, the `AxonVexSystem::RecoveryCallback` typedef, and the stray "Additional methods would continue here..." comment. `ErrorHandler`'s own `RecoveryCallback`/`setRecoveryCallback` are a distinct type and untouched. Verified: 411/411 ctest, zero-warning build. | `system.hpp`, `system.cpp` (fixed) |
| C7 | HIGH | **FIXED.** `emergencyShutdown`/`reset` wrote `currentState_` directly, skipping `transitionState` entirely — no validation, no `totalStateTransitions` count, no STATE_CHANGE event, and (pre-existing) the transition table only allowed `FATAL_ERROR` from `STOPPING`, so a validated e-stop from any other state would have been rejected outright. Fix: `transitionState` now special-cases `newState == FATAL_ERROR` as legal from every state before the switch (the redundant `STOPPING` case entry is dropped); `emergencyShutdown` and `reset` both route through it — `emergencyShutdown`'s call runs before `isShuttingDown_` is set so the STATE_CHANGE event isn't dropped by `publishEvent`'s shutdown guard, and the now-dead `notifyStateChange(oldState, FATAL_ERROR)` call (always dropped for the same reason, pre-fix) is removed. Opened a null-deref trap: `publishEvent` dereferenced `eventPool_`/`eventQueue_` with no guard, and a never-initialized system reaching `emergencyShutdown` now hits it (previously safe only because the raw store skipped notification entirely) — fixed with the same null guard `drainEventQueue` already used. Also removed `initialize()`'s second `initializeComponents()` call (with its `drainEventQueue()` prelude): it rebuilt every component identically to the first call — `initializeComponents` reads only `systemConfig_`, which `loadConfiguration` never touches — and was the sole reason the event pool got replaced mid-`INITIALIZING`, the C25 leak source at this call site. Regression tests `EmergencyShutdownIsAValidatedStateTransition`, `ResetIsAValidatedStateTransition` (reset()'s own `statistics_.reset()` zeroes `totalStateTransitions` immediately after the second transition, so the bump is polled for on a background thread during the ~100ms cleanup sleep instead of read after `reset()` returns), `EmergencyShutdownFromInitializedIsValidated`, `EmergencyShutdownOnUninitializedSystemIsSafe` (pre-fix passes trivially; would segfault post-fix without the null guard). Verified: 408/408 ctest (1 pre-existing flake, passes on rerun), ASan/LSan clean on `AxonVexSystemTest`+`SafetyManagerTest`, TSan clean on `AxonVexSystemTest`. | `system.cpp` (fixed) |
| C8 | HIGH | **FIXED.** Move operations now explicitly `= delete`; compile-time static_asserts lock in non-movability. The `= default` versions were already silently deleted by the compiler (two `std::mutex` members are non-movable), so no code could have moved these objects; the header merely advertised a movability that never existed. Regression tests `ProcessingUnit` non-movability via `std::is_move_constructible`/`std::is_move_assignable` static_asserts. | `processingUnit.hpp`, `ProcessingUnit`'s special member declarations (fixed) |
| C9 | HIGH | **FIXED.** `connectToSystem`/`disconnectFromSystem` locked the two systems' `systemPortsMutex_` in **argument order** (`lock_guard lock1(systemPortsMutex_); lock_guard lock2(targetSystem->systemPortsMutex_);`), so `a.connectToSystem(&b)` running concurrently with `b.connectToSystem(&a)` acquired the pair in opposite orders — classic AB/BA deadlock. Second shape: `targetSystem == this` (a self-connection, output looped back to an input on the same system) locked the same non-recursive `std::mutex` twice — UB, deadlocks in practice. Fix: new private `lockSystemPortsWith(AxonVexSystem*)` returns `std::pair<unique_lock, unique_lock>`; it uses `std::lock(lock1, lock2)` (deadlock-avoiding two-mutex acquisition) for the cross-system case, and locks only `lock1` when the target is `this`, leaving the second lock empty — both functions now call it instead of the raw `lock_guard` pair. Regression tests `OpposingCrossSystemConnectsDoNotDeadlock`, `ConnectSystemToItselfDoesNotSelfDeadlock` (deadline-guarded, worker(s) detached on wedge) — pre-fix both FAIL at their deadline, and the detached threads holding a since-destroyed stack mutex crash or hang the process afterward, as expected for a deliberately-failing pre-fix observation. Verified: 410/410 ctest, TSan clean on the self-connect test; the opposing-connect test's *deadlock* detector is silent post-fix (0 lock-order/deadlock reports) — the 2 data-race warnings it does surface are a separate, pre-existing Logger defect, tracked as C37. | `system.hpp` (fixed) |
| C10 | HIGH | TCP/UDP `::send` without `MSG_NOSIGNAL` → SIGPIPE process kill on peer teardown | `tcpClient.hpp:74,82`, `udpSocket.hpp:77` |
| C11 | HIGH | **FIXED.** Short writes: `::send` never looped on short writes — a partial send was counted as a full message, silently truncating the payload. `TcpClient::sendAll()` now resumes from the offset, retries `EINTR`, and reports how many of how many bytes made it. **Framing (this pass):** the newline delimiter is replaced by a magic+length-prefixed wire format — 6-byte header (`kFrameMagic0`/`kFrameMagic1` = `0xAF 0x01`, then a big-endian `uint32` payload length) followed by exactly that many payload bytes. `TcpClient::send` builds and sends the header before the payload and refuses (reports, returns `false`, never touches the socket) any payload over `kMaxFrameLength` (16 MiB); `TcpClient::recvLoop` incrementally parses complete frames out of the receive buffer, treats a bad magic or an over-`kMaxFrameLength` length as a fatal framing violation: the receive loop terminates and the violation is reported through the error handler (loud `reportError`, no dispatch of unparsed bytes) — the socket itself is not closed and `isRunning()`/`send()` keep reporting the connection alive until the owner calls `stop()` (a half-open ceiling, documented at the `recvLoop` break site; the full fix is deferred to the §6 `axonvex_net` transport work), and handles arbitrary TCP segmentation (byte-at-a-time header/payload splits, multiple frames coalesced into one read, zero-length frames). Breaking: incompatible with any newline-framed peer. Regression tests in `protocolInterfacesContractTest.cpp`: `TcpBinaryPayloadWithNewlineRoundTripsIntact` (pins the exact wire bytes `send()` emits and that a payload containing `0x0A` dispatches as one intact message — pre-fix this split the payload and/or emitted the old newline-delimited bytes), `TcpFramesSurviveSplitAndCoalescedDelivery`, `TcpBadMagicIsLoudErrorAndNothingDispatches`, `TcpOversizeLengthIsLoudErrorAndNothingDispatches`, `TcpSendRefusesOverLimitPayload` (pre-fix this test hung: the old `send()` had no size guard and blocked indefinitely inside `::send()` on an undrained 16 MiB+1 payload). Verified 461/461 ctest, ASan clean, TSan clean (`ProtocolInterfacesContractTest`, `setarch -R` per kernel 6.6+ ASLR requirement), 0 build warnings. | `tcpClient.hpp` (`TcpClient::send`, `TcpClient::recvLoop`, `kFrameMagic0`/`kFrameMagic1`, `kMaxFrameLength`) |
| C12 | HIGH | SafetyManager invokes user callbacks + `triggerEmergencyStop` under `policiesMutex_` (reentrant deadlock); `evaluationPeriod_` read/write race | `safetyManager.hpp:148-177,199-201,224` |
| C27 | HIGH | Over-aligned types (`alignas(64)` in `MemoryPool`, `ThreadSafeQueue`, `RingBuffer`, and the `Block[]`/`Slot[]` arrays) heap-allocated with plain `new`/`make_unique` — C++14's `operator new` only guarantees 16-byte alignment, so every instance was misaligned UB and the false-sharing padding did nothing. UBSan flagged 118 tests. **Fixed**: `AXONVEX_ALIGNED_NEW` + `utils/alignedNew.hpp`. | `memoryPool.hpp`, `threadSafeQueue.hpp`, `ringBuffer.hpp` |
| C28 | MEDIUM | `InputPort`/`OutputPort`/`AsyncInputPort` destructors were `= default`, leaking the object still held in `pooledData_` (the pool frees blocks without running `~T`). `reset`/`setThreadSafe`/`setMemoryPoolSize` already released it; only the destructor did not. **Fixed**: shared `releasePooledData()`. | `ports.hpp:145,220,287` |
| C29 | HIGH | **FIXED.** Ports' pooled read path was a use-after-free, not a missing atomic: `read()` dereferenced `pooledData_` while `writeData()` swapped in a new block and immediately returned the old one to the pool, so a later write recycled and reconstructed the block a reader was still inside. Same shape in `InputPort`, `OutputPort`, `AsyncInputPort`. Fix (approach chosen by the user 2026-08-03): the pooled fast path is **deleted** — the mutex path in the same functions was already correct, and lock-free reads need real reclamation (hazard pointers/epoch) that no benchmark yet justifies. `memoryPool_`/`pooledData_`/`poolSize_` and every pooled branch are gone from all four port classes; the pure-virtual `setMemoryPoolSize`/`getMemoryPoolSize` are removed from `BasePort` rather than left as silent no-ops; `tests/core/memoryPoolPortsTest.cpp` → `tests/core/portThreadSafetyTest.cpp` (mutex-path coverage plus the regression test below); `examples/memory_pool_ports_simple_example.cpp` deleted. `MemoryPool` itself stays — `system.cpp` uses it for `eventPool_`. Regression test `PortThreadSafetyTest.ConcurrentAccessKeepsNonTrivialPayloadIntact` was compiled against the pre-fix header and ASan aborted with heap-use-after-free; it passes post-fix. Un-quarantined from the sanitizer CI gate. | `ports.hpp` (fixed) |
| C30 | HIGH | **FIXED.** `AsyncOutputPort<T>::write()` read `connectedPorts_.empty()` with no lock held, three lines before taking `connectionMutex_` to iterate the same vector — while `connect`/`disconnect`/`disconnectAll` mutated it under that mutex. Unsynchronized read of a `std::vector` being reallocated. Pre-dated the port system's current form (present since `d345805`); found independently during the C29 review and by inspection. Fix: the emptiness check moves into its own critical section, which is released again before `outputCallback_` runs — deliberately *not* one lock spanning the whole function, because that would drag user code under a port lock and add a fifth site to the C12/C18 bug class. The dispatch loop still re-reads under the lock, so a disconnect between the two sections is harmless. | `ports.hpp` (fixed) |
| C31 | MEDIUM | **FIXED.** `BasePort::setThreadSafe` selected between a locked and an unlocked access discipline via an atomic flag, so toggling it while producers/consumers were live raced on the payload itself: a writer that observed `true` took the mutex path concurrently with a reader that observed `false` taking the unlocked path on the same `data_`/`currentData_`. An atomic makes the flag safe, not the switch. Fix: thread safety is now construction-time and immutable — `setThreadSafe` and `ProcessingUnit::setPortsThreadSafe` are **removed** (breaking change), the four port constructors and the four `createXPort` helpers take a trailing `bool threadSafe = false`, and `isThreadSafe_` is a `const bool`. The unsound state transition is now unrepresentable rather than merely documented. | `ports.hpp`, `processingUnit.hpp`, `interfaceUnits.hpp` (fixed) |
| C32 | MEDIUM | **FIXED.** Port callback registration was not thread-safe against dispatch: `outputCallback_`/`dataCallback_`/`validationCallback_` are `std::function` members read unlocked on the write path and written unlocked by their setters, and destroying or replacing a `std::function` while another thread invokes it is a use-after-free. Fix is a contract, not a lock, because both locking alternatives are banned here — a lock on the dispatch path would run user code under a port lock (the C12/C18 class), and copying the function out to invoke it unlocked would allocate on the port hot path. `BasePort::requireNoTrafficYet()` now rejects any registration once the port has carried a message, so the unsafe sequence throws `std::logic_error` at the moment of misuse instead of racing silently. Zero hot-path cost: setters are not hot. Regression test `CallbackRegistrationIsRejectedAfterTraffic`. Note this enforces setup-only registration; it does **not** make concurrent re-registration safe, because that is not a supported operation. | `ports.hpp` (fixed) |
| C33 | MEDIUM | **FIXED.** `start()`/`stop()` on both transports were check-then-act on `running_`, so two concurrent `stop()` calls could both pass the guard and both reach `worker_.join()` — joining an already-joined thread is UB. A `lifecycleMutex_` now serialises the whole lifecycle, making start/stop mutually exclusive and idempotent. **Second shape, caught in review:** message and error callbacks run *on the worker thread*, so a callback calling `stop()` made the worker join itself — `join()` on the calling thread throws `system_error("Resource deadlock avoided")`, and from inside a callback that escapes `recvLoop` and the thread entry, so `std::terminate` killed the process (reproduced: exit 134). `stop()` now detects the self-call, leaves `running_` false so the loop unwinds on its own, and defers the join/close to the destructor or a later external `stop()`; the early return on `running_` was removed so that deferred teardown is still reachable. Both transports also gained destructors — they had none, so destroying a running transport ran `~std::thread` on a joinable thread, another `std::terminate`. Regression tests `ConcurrentStopIsSafe` and `StopFromCallbackDoesNotSelfJoin`. | `tcpClient.hpp`, `udpSocket.hpp` (fixed) |
| C34 | HIGH | **FIXED.** Both transports invoked user callbacks while holding `cbMutex_` (`recvLoop` message dispatch, `reportError` handler dispatch) — the C12/C18 class in the interfaces layer, and what made C33's self-join reachable. Naively snapshotting and dispatching unlocked would have traded the deadlock for the C18 crash: the snapshot holds raw `Callback*` the transport does not own, so an unregister racing dispatch dangles them. New `detail/dispatchBarrier.hpp` keeps both properties — dispatch snapshots under the lock, marks itself in flight, releases the lock before user code; an unregister from another thread waits for in-flight dispatch to drain, while one issued *from inside a callback* returns immediately (recording the dispatching thread, exactly as `SafetyManager` does with `evaluatingThread_` in C12) so it cannot wait on itself. Documented consequence: a handler that unregisters itself may still be called for the remainder of the dispatch already under way. `CallerKeyed` gained a public `snapshotCallbacksForKey`. **Second shape, caught in review:** the first barrier tracked a single "current dispatcher" `thread::id`, which is unsound because one transport shares the barrier between the message path (receive thread) and the error path (reachable from `send()` on any caller thread) — two threads can dispatch at once, and one id only remembers the later. The thread that entered first then failed its own self-check, waited for the in-flight count to drain, and blocked forever on its own dispatch. Replaced with a per-thread depth map, which also doubles as the recursion counter. (`SafetyManager`'s `evaluatingThread_` can be a single id only because C12 serialises evaluation cycles first; this barrier deliberately does not serialise dispatch.) Regression tests `CallbackMayReenterRegistrationApi` and `ConcurrentDispatchFromTwoThreadsDoesNotDeadlock` — pre-fix both hang their deadlines and fail. | `tcpClient.hpp`, `udpSocket.hpp`, `detail/dispatchBarrier.hpp` (fixed) |
| C35 | HIGH | **FIXED.** `OutputPort::write` and `AsyncOutputPort::write` dispatched to their connected ports while holding `connectionMutex_`, and that call runs the receiving port's user callback — so a downstream callback touching the sender's connection API deadlocked on a non-recursive mutex. Flagged repeatedly during the C29/C30 reviews and deferred as "a design call" because copying the connection vector per write would allocate on the port path. Resolved without either cost: the connection list is now copy-on-write behind a `shared_ptr<const vector>`, so the write path takes one refcount bump under the lock, releases it, and dispatches off an immutable snapshot that stays alive even if a concurrent disconnect replaces it. Allocation moves to `connect`/`disconnect`, which are setup operations. This also retires C30's two-critical-section workaround — the emptiness check and the dispatch now read the same snapshot and cannot disagree. Regression test `DownstreamCallbackMayReenterConnectionApi` — pre-fix it deadlocks and fails on the deadline. | `ports.hpp` (fixed) |
| C26 | MEDIUM | **FIXED.** No try/catch around user code on either safety thread entry: a policy whose `evaluate()` threw, a watchdog handler that threw, or a throwing `SafetyHook` emergency callback all unwound out of their worker thread and called `std::terminate` — killing the process whose safety these components exist to preserve. Guarded at all four sites. A throwing policy is deliberately **not** swallowed: a policy that cannot report cannot vouch for the system, so treating the throw as NOMINAL would be the silent-failure class this project bans. It is reported as `CRITICAL` — enough to dominate the cycle's worst level and surface through the normal event path, deliberately short of `EMERGENCY` so a malfunctioning policy cannot trip the e-stop by itself. The cycle continues to the remaining policies, and the loop keeps running. Regression tests `ThrowingPolicyIsReportedNotFatal`, `ThrowingHandlerDoesNotKillTheWatchdog` — pre-fix both abort with SIGABRT (exit 134). | `safetyManager.hpp`, `watchdog.hpp` (fixed) |
| C36 | HIGH | **FIXED.** `SafetyManager::stop()` and `Watchdog::stop()` carried C33's self-join defect — the third and fourth occurrences in this codebase. Policies, event handlers and watchdog timeout handlers all run *on* the worker thread, so any of them calling `stop()` made that thread join itself: `system_error("Resource deadlock avoided")` unwinding out of the thread entry, i.e. `std::terminate` (reproduced, exit 134). Fixed as in the transports — detect the self-call, leave `running_` false so the loop unwinds, defer the join. **The self-detection itself then had to change everywhere, including the already-shipped transports:** reading `worker_.get_id()` races `start()`'s move-assignment of `worker_` (TSan-confirmed once a test finally exercised it), and in the transports the check sat *after* `lifecycleMutex_`, which an external `stop()` holds while waiting to join — a callback's `stop()` would have blocked on it forever. The worker now publishes its own id into an `atomic<thread::id>` as its first act, and `stop()` checks that before touching either the mutex or the `std::thread`. **Two further CRITICALs, caught in review of the first attempt:** (a) `~Watchdog` had no detach fallback, so destroying the object from its own handler left `worker_` joinable and `~std::thread` reached `std::terminate` — the same shape one level up; the transports already had this fallback and the safety classes did not. (b) Removing the `running_` check-then-act (necessary to make a deferred self-stop reachable) also removed the only thing serialising two *external* `stop()` calls, so both could clear the self-check and both reach `join()` on the same `std::thread` — UB, and it hangs in practice. Both safety classes now carry a `lifecycleMutex_` around the join tail, taken **after** the self-check for the deadlock reason above. Regression tests `PolicyMayStopTheManager`, `HandlerMayStopTheWatchdog`, plus `ConcurrentStopIsSafe` on both classes (pre-fix both hang and are killed at the 90 s timeout). `workerId_` is also cleared by the worker as its last act, since a `thread::id` is reusable once its thread exits and a stale one would let an unrelated future thread wrongly skip the join. | `safetyManager.hpp`, `watchdog.hpp`, `tcpClient.hpp`, `udpSocket.hpp` (fixed) |
| C37 | MEDIUM | **FIXED.** Found during C9 verification. `LogOutput::defaultFormat` (`logger.hpp:750`) called `std::strftime(..., std::localtime(&time_t))` — `std::localtime` is non-reentrant (glibc: one shared static `struct tm`, not even thread-local), so two `AxonVexSystem`s' independent Logger background threads formatting messages at the same moment raced on libc's internal state. TSan-confirmed by `AxonVexSystemTest.OpposingCrossSystemConnectsDoNotDeadlock` (2 data-race reports rooted in `defaultFormat`/`tzset_internal`; the same test's lock-order/deadlock detector was otherwise silent — a distinct bug from C9). One of the TSan stacks showed `free()` inside `tzset_internal` in the racing window — this is a race on **libc-internal state, including allocator activity from `tzset`'s lazy timezone-data reload**, not merely garbled output characters; a torn read into `strftime`'s output buffer was the *best* case, a concurrent glibc-internal `malloc`/`free` on the same shared state was the actual exposure. First test in the suite to run two `AxonVexSystem` Loggers concurrently under TSan, so latent until now, and every additional concurrent-system test risked hitting it. Fix: `localtime_r` (POSIX) in place of `std::localtime`, writing into a caller-owned `std::tm` instead of the shared static buffer; `#ifdef _WIN32` guards `localtime_s` (which takes the destination `tm*` first, source `time_t*` second — reversed from `localtime_r`) for the non-POSIX target rather than silently no-opping. Grepped the rest of `src/` for other `localtime`/`gmtime` call sites — the only other occurrence is dead, commented-out code in `ColoredStreamLogger`'s destructor (`logger.hpp:915`), not fixed since it does not execute. Verified: `setarch $(uname -m) -R ctest --test-dir build-tsan -R AxonVexSystemTest` green (45/45) across 3 runs; the previously-failing test run directly 4/4 with exit code 0 and zero TSan report lines each time (was 2 data-race warnings / exit 66, reproduced 4/4 pre-fix). Full plain-build ctest also green (410/410). | `logger.hpp:750` (fixed) |
| C13 | HIGH | **FIXED.** Four shapes, all in the plugin loading path (not the ROS2 adapter internals — `registerDefaultCasters`/type-caster machinery is C20's scope, untouched here). (a) `axonvex_ros2` was an `INTERFACE` library and its three `extern "C" inline` factory functions lived in the header — no TU ever compiled them, so no `.so` was ever produced and nothing was dlsym-able; `find build -name 'libaxonvex_ros2*'` returned nothing pre-fix on every build dir (`build`, `build-asan`, `build-tsan`) even with rclcpp visible. (b) No ABI/version handshake: a plugin built against a stale `PluginInterface` layout would `dlopen` and `dlsym` fine and only fail (or corrupt memory) at the first virtual call. (c) `PluginManager::loadPlugin`'s dup-name `plugins_.emplace(...)` silently discarded the new instance while returning `true` — the caller believed it loaded, and the instance + dlopen handle leaked (ASan/LSan-confirmed: 3 leaked allocations from `axonvex_create_plugin` in a two-line repro, one of them purely from `~PluginManager` never existing to release a *non-duplicate* load either). (d) `dlopen` used bare `RTLD_NOW`, leaving the LOCAL/GLOBAL scope implementation-defined. Fix: `axonvex_ros2` is now `add_library(... SHARED src/ros2Plugin.cpp)` with the three non-inline `extern "C"` definitions in that one TU (`nm -D` confirms `T axonvex_create_plugin`, `T axonvex_destroy_plugin`, `T axonvex_plugin_abi_version`); `pluginInterface.hpp` adds `AXONVEX_PLUGIN_ABI_VERSION` (value 1) with a doc contract that every plugin exports `extern "C" uint32_t axonvex_plugin_abi_version()`; `PluginManager::makePosixLoader` calls `dlopen(path, RTLD_NOW | RTLD_LOCAL)`, checks the ABI symbol via `dlsym` *before* calling `axonvex_create_plugin`, and `dlclose`s + reports a reason on any failure (missing symbol, ABI mismatch, throw, null instance); `loadPlugin` checks `emplace`'s `.second` and on a duplicate name calls `shutdown()`/`destroy()`/`dlclose()` on the rejected instance before returning `false`; a new `PluginManager::lastError()` replaces the banned bare-`false`-for-four-causes shape. Also closed while in this scope: `~PluginManager()` did not exist, so a `PluginManager` going out of scope with plugins still loaded leaked every instance/handle regardless of the dup-name path — added a destructor that calls `unloadAllPlugins()`, and made the class non-copyable (copying would double-`dlclose`/double-`delete` the same owned resources) with defaulted move. **Breaking:** out-of-tree plugins now require exporting `axonvex_plugin_abi_version`; one without it fails to load with a reason via `lastError()` instead of loading. Test-bed: `tests/plugins/fixture/testPlugin.cpp`, one TU built as three MODULE targets (`axonvex_test_plugin`, `_oldabi` via `-DAXONVEX_TEST_PLUGIN_BAD_ABI`, `_nosymbols` via `-DAXONVEX_TEST_PLUGIN_NO_SYMBOLS`) exercised by `tests/plugins/pluginManagerTest.cpp` (`LoadsRealSharedObjectAndCallsFactory`, `DuplicateNameIsRefusedWithoutLeak`, `AbiMismatchIsRefused`, `MissingSymbolsAreRefused`). Verified: 436/436 ctest green (`build`), ASan/LSan clean on `PluginManagerTest` including the dup-name test (`build-asan`), TSan clean (`build-tsan`), UBSan clean (`build-ubsan`, plugins off there — fixtures still build/pass), 0 warnings across all four configs; `nm -D` on `libaxonvex_ros2.so` shows all three symbols with `T` visibility in `build`, `build-asan`, and `build-tsan`. **Whole-branch-review findings 2 & 3, folded into this row:** (2) `loadPluginsFromDirectory()`'s `lastError()` was meaningless after a multi-file scan — `loadPlugin()` resets `lastError_` on every call and `readdir()` order is unspecified, so a later success could erase an earlier failure's reason. It now accumulates every failed file's `"name: reason"` into `lastError_` after the loop, regardless of enumeration order (test `ScanAggregatesFailureReasonsRegardlessOfOrder`). (3) `loadPlugin()`'s call to `lr.instance->initialize()` was the one factory call in the function not wrapped in `try`/`catch`, unlike its `abiVersion()`/`create()` siblings — a throwing `initialize()` propagated out (breaking the bool+`lastError()` contract) and leaked the instance and dlopen handle (LSan-confirmed pre-fix: 8 bytes from `axonvex_create_plugin` never freed). Now caught, cleaned up (`destroy()` + `dlclose()`), and reported via `lastError()` as `"plugin initialize() threw"`. New 4th fixture build mode `axonvex_test_plugin_throwinit` (`-DAXONVEX_TEST_PLUGIN_THROW_INIT`) backs the regression test `ThrowingInitializeIsCaughtWithoutLeak`; ASan/LSan clean post-fix (436+ ctest green including both new tests). | `plugins/axonvex_ros2/CMakeLists.txt`, `plugins/axonvex_ros2/src/ros2Plugin.cpp`, `pluginInterface.hpp` (`AXONVEX_PLUGIN_ABI_VERSION`), `pluginManager.hpp` (`PluginManager::makePosixLoader`, `PluginManager::loadPlugin`, `PluginManager::lastError`, `PluginManager::~PluginManager`, `PluginManager::loadPluginsFromDirectory`) (fixed) |
| C14 | HIGH | **FIXED.** `Logger::formatString` was `return format;` — every `logf`/`LOGF_*` argument was silently dropped (the banned silent-failure shape, on a safety framework's logging API). `LOGF_CRITICAL` expanded to two concatenated `logf` calls (`(logger).logf(...)(logger).logf(...)`), a malformed double-expression that failed to compile at any use site — nothing used it, which is why the tree built. Fix: a new private `Logger::formatImpl` overload pair (a variadic template plus a no-args base case) substitutes each `{}` in `format` in order via `operator<<` on an `ostringstream`, called by `formatString`; hand-rolled (no `fmt` dependency), deliberately not RT-safe (allocates) since `logf` is a convenience API, not a hot-path one. Contract: extra `{}` with no matching argument stays literal; extra arguments with no matching `{}` are silently unused — both documented at `formatImpl`. `LOGF_CRITICAL` now expands to exactly one `logf` call, matching the other `LOGF_*` macros. The header's `<500ns per log entry`/`no dynamic allocation during logging` doc-comment claims are reworded to state what is true today: the hot path enqueues to a pre-allocated message pool but builds `std::string`s per call (allocates), so `logf`/`log` are not RT-safe. Regression tests `LogfSubstitutesPlaceholdersInOrder`, `LogfLeavesUnmatchedPlaceholdersLiteral`, `LogfIgnoresExtraArguments` (all failed pre-fix: message contained the literal `{}` format, not substituted values) and `LogfCriticalMacroCompilesAndLogsOnce` (its own regression is that it failed to *compile* pre-fix — `void value not ignored as it ought to be`). Verified: 24/24 `LoggerTest` green, 415/415 full ctest green, ASan `LoggerTest` clean except the pre-existing `HighLoadPerformance` throughput assertion (unrelated: exercises `logger.info()`, not `logf`/`formatString`, and fails only under ASan's instrumentation overhead — the same category as `MemoryPoolTest.PerformanceTest` under TSan, above). | `Logger::formatString`, `Logger::formatImpl`, `LOGF_CRITICAL` (fixed) |
| C15 | MEDIUM | **FIXED.** Four shapes. (a) `Path::Path(...)`'s three ctors checked a plain `bool initialized_` before taking `static_mutex_` — broken double-checked locking: a racing second thread could observe `initialized_ == true` while `default_dirs_`'s writes from `initializeDefaultDirs()` were not yet visible to it. (b) `Path::createConfigPath`/`Path::createLogPath` computed `filename.substr(filename.length() - N)` for the extension check; for names shorter than the extension (`"a"`, N=5) `length() - N` underflows `size_t` to a huge value and `substr()` throws `std::out_of_range`. (c) `Path::hasDirectoryTraversal()` substring-matched `".."`/`"./"`/`".\\"`, flagging legitimate names that merely contain those characters (`"my..file.json"`, `"./config/x.json"`) while being no harder to construct an actual escape around. (d) `Path::isInWhitelist()` iterated `default_dirs_` with no lock. Fix: (a) `Path::ensureInitialized()`, a C++11 magic static, replaces the three ctors' DCLP — exactly-once execution with the standard's synchronization guarantee; the lambda still takes `static_mutex_` around the `initializeDefaultDirs()` call because grep confirms `default_dirs_` has two post-init writers (`setDefaultDir`, `resetDefaultDirs`, both already under `static_mutex_`) that this write must serialize against. (b) both functions guard with `filename.length() < N` before calling `substr`. (c) component-based check: traversal iff a path component is exactly `".."`, computed by splitting `toString()` manually on both `/` and `\` (not `axonvex_fs::path`'s own iteration, which is native-separator-only and would miss a Windows-separator escape attempt on a POSIX build) — every real `".."` component (`"a/../b"`, `".."`, `"../x"`, `"a\\..\\b"`) is still flagged; only the substring false positives are fixed. (d) `isInWhitelist()`'s iteration now takes `static_mutex_`, matching (a)'s finding that `default_dirs_` is mutable after init. Regression tests `CreateConfigPathAcceptsShortNames`, `TraversalCheckIsComponentBased` (positive and negative cases). (a)'s first-touch initialization race has no deterministic in-suite repro (by the time any test runs, an earlier test has already forced the one-time init) — verified by inspection plus the full suite, including this file's own `ThreadSafetyTest` (8 threads constructing `Path`s concurrently), TSan-clean. Verified 429/429 ctest, ASan clean on `PathTest`, TSan clean on `PathTest` (no race/deadlock reports; `PathTest.PerformanceTest`'s hardcoded ns-threshold fails under TSan's instrumentation overhead, same pre-existing class as `MemoryPoolTest.PerformanceTest` — not a regression, unrelated to correctness). **Behavior changes:** `createConfigPath`/`createLogPath` no longer throw on short names (extension is appended instead); a path containing `".."` as a substring of a component or a `./` prefix is no longer flagged as traversal — a real `".."` component still is. | `path.cpp` (`ensureInitialized`, `createConfigPath`, `createLogPath`, `hasDirectoryTraversal`, `isInWhitelist`) (fixed) |
| C16 | MEDIUM | `optional` move ops destroy the source (non-std semantics) | `optional.hpp:44-96` |
| C17 | MEDIUM | UUID v4 version/variant bits applied to the wrong 64-bit halves → not RFC-4122-compliant strings | `uuid.hpp:27-31,69-70` |
| C18 | MEDIUM | Callbacks invoked under locks (recurring pattern): `executeTask` error callback under `tasksMutex_`+`statsMutex_` + `std::cerr`; `performHealthCheck` under `callbacksMutex_`; `lastHealth_` read race | `timingController.cpp:524-564`, `system.cpp:579-596,1195` |
| C19 | MEDIUM | **FIXED.** `PrecisionTimer::addSample` did `samples_.erase(samples_.begin())` at capacity — an O(n) shift of up to a 10k-element vector under `samples_mutex_` on every measured execution. `ClockType` aliased `high_resolution_clock`, which the standard permits to be non-steady (a wall-clock jump would corrupt elapsed-time measurements); on this toolchain it measurably was non-steady (`isClockSteady()` returned `false` pre-fix). `getElapsedNanoseconds` threw `std::runtime_error` when the timer had never been started — a timing getter reachable from RT-adjacent paths must not throw. Fix: `ClockType` is now `std::chrono::steady_clock` (`isClockSteady()` always `true`); `addSample` ring-writes into `samples_` via a new `sampleHead_` index once at capacity (O(1)), reset to 0 in `reset()`/`clearSamples()` (no runtime max-samples setter exists, so no further reset site); `getElapsedNanoseconds` returns `DurationType::zero()` instead of throwing, and `getElapsedSeconds`/`Milliseconds`/`Microseconds` inherit the fix for free since they all delegate to it (no independent throw sites found). **Contract change:** callers relying on `getElapsedNanoseconds()`/`getElapsedMicroseconds()`/etc. throwing pre-start must now check for zero instead of catching `std::runtime_error`. Regression tests: `SampleEvictionKeepsMostRecentSamples` (feeds a small-capacity timer cheap-then-expensive laps and asserts the retained set is entirely from the expensive batch — passes both pre- and post-fix, since eviction order was already correct and only its complexity changed), `ElapsedGettersReturnZeroBeforeStart` and `ClockIsSteady` (both RED pre-fix: the former threw, the latter asserted `false`), `ErrorHandling` updated in place to the new zero-return contract. Verified: 22/22 `PrecisionTimerTest` green, 418/418 full ctest green, ASan clean on `PrecisionTimerTest`. | `precisionTimer.cpp` (fixed), `precisionTimer.hpp` (fixed) |
| C20 | MEDIUM | **FIXED.** Four shapes. (a) `TypeCasterRegistry` keyed `casters_` on `typeid(InternalType)` alone: `registerCaster<float, Float32>` then `registerCaster<float, Float64>` silently overwrote the first entry, and a subsequent `getCaster<float, Float32>()` `dynamic_cast`'ed the surviving `Float64` caster to the wrong type and returned a silent `nullptr` — indistinguishable from "never registered". (b) `createServer` built `T data{}` and pushed the default-constructed value, discarding the actual request payload (`(void)request;` right next to it). (c) `registerDefaultCasters()` was an empty body under a doc comment claiming four primitive casters were pre-registered — a `SubscriberUnit<std::string>`/`Float32` etc. app would compile, run, and silently never receive data. (d) All four factories (`createSubscriber`/`createPublisher`/`createServer`/`createClient`) returned raw owning `new` pointers with no ownership contract enforced. Fix: `TypeCasterRegistry` now keys on `std::pair<std::type_index, std::type_index>` (InternalType, RosMsg) via a `TypeIndexPairHash` two-value combine (C++14 has no `std::hash<pair>` — four lines, no dependency); `hasCaster`/`getRawCaster` re-keyed to the same templated exact-pair lookup (had zero callers anywhere in-tree, so no call-site migration needed). `createServer<T, RosSrv>` now takes a required `std::function<T(const typename RosSrv::Request&)> requestMapper` — a null mapper throws `std::invalid_argument` instead of silently discarding requests. **Response handling is intentionally not added**: `axonvex::core::ServerUnit<T>` (`interfaceUnits.hpp`) has no response path at all — `pushRequest()` only fans the mapped value out to output ports for the pipeline to read; there is no callback or port that feeds a value back into the RPC call. A response-mapper parameter would be speculative until `ServerUnit` itself grows a response port, so every service call still completes with a default-constructed `RosSrv::Response`, documented honestly in `createServer`'s doc comment instead of pretended away. `registerDefaultCasters()` is now real: a new opt-in `defaultCasters.hpp` registers `float<->std_msgs::msg::Float32`, `double<->Float64`, `bool<->Bool`, `std::string<->String`, compiled only under `AXONVEX_ROS2_HAS_STD_MSGS` (set by `plugins/axonvex_ros2/CMakeLists.txt` after a real `find_package(std_msgs QUIET)` success, never assumed); without std_msgs, `ROS2Adapter::registerDefaultCasters()` does not exist as a method at all rather than compiling to an empty body that claims work it didn't do. `createSubscriber`/`createPublisher`/`createServer`/`createClient` all return `std::unique_ptr<XxxUnit<T>>`; the null-caster path in `createSubscriber`/`createPublisher` was already a loud `throw std::runtime_error` pre-fix (verified, not changed in kind — message now also names the `RosMsg` type, not just `InternalType`), so no silent-no-op shape existed there to close. **Breaking:** the four factories return `unique_ptr` (was raw `new`); `createServer` requires a non-null `requestMapper` argument. **Breaking (final review wave):** the constructor no longer auto-calls `registerDefaultCasters()` — it is `public` and opt-in, call it yourself; `createClient<T, RosSrv>` now also requires a non-null `std::function<typename RosSrv::Request(const T&)> requestMapper` (it had the identical silent-default-Request-drop shape `createServer` was fixed for, missed in the first pass — see the Status paragraph below). Test-bed: `tests/core/typeCasterRegistryTest.cpp` (`typeCasterRegistry.hpp` is standalone — no rclcpp include — so it joins the main `test_core` binary, not a plugin-gated target; verified by inspection of its `#include`s) — `DistinctRosTypesForOneInternalTypeCoexist` (RED pre-fix: second registration overwrote the first, first lookup returned `nullptr`), `UnregisteredPairReturnsNull`. A new plugin-gated `test_ros2_adapter` (only built when `axonvex_ros2` itself builds) exercises the real Humble headers end-to-end: `NodeConstructionWorks` (rclcpp::init()/Node construction genuinely runs here, no ROS daemon needed), `RegisterDefaultCastersMakesStdStringCasterAvailable`, `DefaultCastersAreNotRegisteredWithoutAnExplicitCall`, `MissingCasterThrowsInsteadOfSilentlyFailing`, `ServerMapsRequestThroughToServerUnit`, `CreateClientCompilesAndConstructs`, `CreateClientRejectsNullRequestMapper`, `ClientMapsRequestThroughToWire`. Found during this test's first genuine `rclcpp::init()`/`shutdown()` pair under ThreadSanitizer: a pre-existing double-lock in `spdlog::details::periodic_worker`'s destructor (rclcpp's internal async logger, vendored via `libspdlog.so.1`) at shutdown — reproduces from `rclcpp::init()`+`shutdown()` alone, independent of anything in this diff, so `test_ros2_adapter` is compiled but not registered as a ctest under `AXONVEX_SANITIZER=thread` (documented in the CMake comment, not silently dropped). Verified: 447/447 ctest green (`build`), `TypeCasterRegistryTest` and `Ros2AdapterSmokeTest` (5/5, including the genuine rclcpp smoke check) ASan-clean (`build-asan`), `TypeCasterRegistryTest` UBSan-clean (`build-ubsan`, plugins off there per C13 precedent), 0 warnings across `build`/`build-asan`/`build-ubsan`/`build-tsan` for every touched file. | `typeCasterRegistry.hpp` (fixed), `ros2Adapter.hpp` (fixed), `defaultCasters.hpp` (new), `plugins/axonvex_ros2/CMakeLists.txt` (fixed), `tests/core/typeCasterRegistryTest.cpp` (new), `tests/plugins/ros2AdapterSmokeTest.cpp` (new) |
| C21 | MEDIUM | **FIXED.** `RingBuffer`'s ctor called `std::make_unique<T[]>(capacity_)` (already value-initializes every element) and then looped `new (&buffer_[i]) T{}` over the live result: placement-new ends an object's lifetime without running its destructor, so every slot was constructed twice and destroyed once — a leak of the first object's resources for any non-trivial `T`. The class doc also claimed generic "lock-free" for an algorithm that is SPSC-only: two concurrent writers can both load the same `write_index_` and both store to `buffer_[current_write]` (symmetrically for two readers on `read_index_`). Fix: the placement-new loop is deleted (`RingBuffer`'s ctor now trusts `make_unique<T[]>`'s value-initialization outright); the class doc states the honest single-producer/single-consumer contract and names the both-writers-claim-same-slot failure mode; a memory-ordering argument comment sits next to `write_index_`/`read_index_` (each index is release-stored by its own thread after touching the slot, acquire-loaded by the *other* thread before touching that slot — same argument shape as `ThreadSafeQueue`'s per-slot `sequence`, specialized to two indices). Regression test `RingBufferTest.ConstructionAndDestructionAreBalanced` (pre-fix: 32 constructions vs 16 destructions for a 16-element buffer — exactly 2x, confirming the double-construct); new SPSC stress test `RingBufferTest.SpscStressPreservesValueIntegrityAndOrder` (one producer, one consumer, 200k items, deadline-guarded, value+order integrity) backs the lock-free claim per project rule 8. Verified: 424/424 ctest, ASan/LSan clean, TSan clean on `RingBufferTest` (3 consecutive stress reruns, zero report lines). | `ringBuffer.hpp` (fixed) |
| C22 | MEDIUM | **FIXED.** `MissionPipeline::status_`/`currentElement_`/`elements_`/`transitions_`/`startElementName_` were mutated by `processSync()` (scheduler thread) and by `startPipeline`/`abort`/`pause`/`resume`/`restart`/`addElement`/etc. (any user thread) with no synchronization — TSan confirmed two data races pre-fix (`pause()` vs. `processSync()` on `status_`; `currentElementName()` vs. `processSync()` on `currentElement_`). Ownership of the raw `MissionElement*` passed to `addElement()` was undocumented, and a transition naming a nonexistent element (a typo) was only ever discovered as a silently wedged pipeline at runtime. Fix: one non-recursive `mutable std::mutex stateMutex_` now guards all five members. Every control method (`startPipeline`/`abort`/`restart`/`pause`/`resume`/`reset`) follows lock → decide + apply the state flip on a captured `MissionElement*` snapshot → unlock → invoke the hook (`onEnter`/`onExit`/`reset`) on the snapshot — hooks are user code and are never called with `stateMutex_` held, so a hook that re-enters the control API (e.g. `onEnter()` calling `pause()`) cannot deadlock. `processSync()` takes the lock up to three times per tick, each time only for a map lookup/state-flip, never across `execute()`/`onEnter()`/`onExit()`; each post-`execute()` re-lock re-checks `currentElement_`/`status_` are unchanged before applying its transition, so a concurrent `abort()`/`pause()`/`restart()` racing the unlocked `execute()` call wins instead of being silently overwritten. `addElement()` now documents the non-owned, stable-reference ownership contract. New private `validateGraph()` (called under the lock from `startPipeline()`) checks the start element and every `transitions_` from/to key name a real element, and on failure sets `status_ = Failed` and surfaces the first dangling reference through `ProcessingUnit::setError()`/`getLastError()`. Regression tests: `ConcurrentControlAndTickAreRaceFree` (TSan is the real assertion — a tick thread and a pause/resume/status/currentElementName thread race for 1s against a cyclic 3-element graph), `StartPipelineRejectsDanglingTransition` (pre-fix RED: `startPipeline()` only checked the start element, so status landed `Executing`, not `Failed`), `HookReenteringControlApiDoesNotDeadlock` (an `onEnter()` hook calls `pause()`; deadline-guarded, `FAIL()`s instead of hanging the suite on a naive lock-across-hooks regression). Verified: 432/432 ctest green, TSan clean (race test + full suite excluding the pre-existing sanitizer-slowdown quarantine list), ASan clean, 6-process × 6-repeat parallel-load gate on the three new tests (36/36 green, 0 failures). **Correction (whole-branch review, finding 1):** the row above described `stateMutex_` as the whole fix, but missed that the hook dispatch itself — `onExit`/`reset`/`onEnter` after the unlock — still ran on whichever thread called `abort`/`restart`/`pause`/`resume`/`reset`/`startPipeline` directly, which could still run concurrently with `processSync()`'s unlocked `execute()` on the SAME `MissionElement` (TSan-reproduced: `onExit()` write racing `execute()` write). `abort`/`restart`/`pause`/`resume`/`reset` now only enqueue (the existing `controlPort_` for the first four, a dedicated `pendingReset_` flag for `reset()` — see its doc comment for why); `startPipeline()`'s bookkeeping stays synchronous and fail-fast but its `onEnter()` dispatch is deferred the same way. Every one of those hooks now runs from `processAsync()`'s drain only, so there is exactly one thread that ever calls a `MissionElement` hook. Honest contract: these methods take effect at the next tick, not synchronously — `status()`/`currentElementName()` are still plain synchronous reads, but they reflect the most recently *drained* command, not the most recently issued one. New regression test `DirectAbortDoesNotRaceConcurrentTick` (TSan-reproduced pre-fix, TSan-clean post-fix); `ConcurrentControlAndTickAreRaceFree` and `HookReenteringControlApiDoesNotDeadlock` updated for the deferred semantics. **Correction (batch-6 review, item 1):** the previous correction still left `startPipeline()`'s Idle->Executing flip synchronous while `onEnter()` was deferred, so a `processSync()` (or a queued `reset()`) landing between the two could call `execute()`/`onExit()` on an element that had never been entered — the same pairing hazard `abort`/`restart`/`pause`/`resume`/`reset` had already been fixed for, missed for `startPipeline()` itself. Fix: the flip is deferred into the drain too, performed together with `onEnter()` by `startImmediate()` (shared with `restartImpl()`'s re-start path); `status()`/`currentElementName()` now read `Idle` immediately after a successful `startPipeline()` call, not just after its `onEnter()`. New regression tests `StartPipelineDefersFlipSoExecuteNeverPrecedesEnter` (RED pre-fix: `execute()` ran with `enterCount()==0`) and `ResetQueuedBeforeDrainDoesNotExitNeverEnteredElement` (RED pre-fix: a queued `reset()` fired `onExit()` on a never-entered element); every pre-existing test asserting `Executing` synchronously after `startPipeline()` updated to tick-then-assert. Verified: 451/451 ctest green, TSan/ASan clean on the mission suite, 6-process × 6-repeat parallel-load gate on `MissionPipelineTest` (36/36 green, 0 failures). Same pass also cleared `PluginManager::loadPluginsFromDirectory()`'s stale-`lastError_`-on-early-return gap (C13's area) and corrected `dispatchDeferredEnter()`'s drain-order comment. | `missionPipeline.hpp`, `pluginManager.hpp` (fixed) |
| C23 | LOW | `nextPowerOf2` `value >> 32` UB on 32-bit `size_t` (3 copies of the function) | `coreUtilities.hpp:36-45`, `memoryPool.hpp:247-257`, `threadSafeQueue.hpp:234` |
| C24 | LOW | **FIXED.** Fixed: the TCP/UDP fd close race. `stop()` closed the socket while the receive thread was blocked in `recv`/`recvfrom` on it and `send()` read `sock_` with no guard — TSan reported races on `sock_`, on the descriptors themselves, and on the plain-`uint64_t` `ProtocolStatistics` counters shared by the send and receive threads. Closing an fd another thread is blocked on is worse than a torn read: the number is immediately reusable, so the blocked call can end up on a stranger's descriptor. Fix: `SO_RCVTIMEO` (100 ms) bounds the receive block so the loop exits on `running_` by itself; `stop()` joins *before* closing (TCP additionally calls `shutdown()` first, which wakes a blocked `recv` without invalidating the fd); `send()` holds a new `sockMutex_` across the syscall; UDP peer state moves under `peerMutex_`; counters move to a new `AtomicProtocolStatistics` with a plain-struct `snapshot()` so callers are unaffected. Regression test `UdpConcurrentSendAndStopIsRaceFree` reproduced all five races against the pre-fix headers. Un-quarantined — no known race remains in the sanitizer gate. **Now also fixed:** address resolution moved to `getaddrinfo` via the new shared `detail/addressResolver.hpp` — hostnames, IPv4 and IPv6 all work, candidates are tried in the system's preference order, and UDP picks a destination matching the bound socket's family. The non-Linux stubs no longer no-op and return `true`: `start()`/`send()` report through the error channel and return `false` (`reportError` moved outside the platform guard for this). Regression tests `UdpResolvesHostnamesAndIpv6` and `UdpUnresolvableHostFailsWithError`. **C24 is fully closed.** | `udpSocket.hpp`, `tcpClient.hpp` (race fixed); DNS/IPv6/non-Linux stubs open |
| C25 | HIGH | `updateSystemConfiguration` writes `systemConfig_` with no synchronization while monitoring/event/scheduler threads read it (TSan-confirmed during C18 verification); also `publishEvent` after the event thread stops leaks the event's strings (pool objects constructed, never destroyed — LSan-confirmed) | `system.cpp:1582`, `system.cpp` `publishEvent`/`emergencyShutdown` ordering |
| C38 | MEDIUM | **FIXED.** Found during C37 review. `isShuttingDown_` (declared in `system.hpp`) is set by `stop()`/`emergencyShutdown()` but was never cleared anywhere — `reset()` tore the system down to `SystemState::UNINITIALIZED` without resetting the flag. A subsequent `initialize()` on the same object rebuilt `eventProcessingThread_` and called `eventProcessingLoop()`, whose loop guard is `while (eventProcessingRunning_.load() && !isShuttingDown_.load())` (`system.cpp`) — the stale `true` made the loop body never execute even once, so the newly re-initialized system's event thread exited immediately. Worse, `publishEvent` (`system.cpp`) unconditionally dropped every event once `isShuttingDown_` was set, so the re-initialized system silently discarded all events with no error, no log, no queue-full/pool-exhausted signal — a silent-failure shape the project otherwise guards against (C25's shutdown-time drop guard was deliberately meant to be *temporary*, scoped to the shutdown window, not permanent). State machine reported a healthy `INITIALIZED`/`RUNNING` system with a dead event pipeline. Fix: `initialize(const std::string&)` clears `isShuttingDown_` immediately before the monitoring/event threads start (after `initializeBlocksLayout()` succeeds), making `initialize()` the single owner of clearing the flag — `reset()` deliberately does not also clear it, so a racing `stop()`/`emergencyShutdown()` cannot be un-done by a second clearer. Verified with regression test `EventPipelineIsAliveAfterResetAndReinitialize` (pre-fix: failed on the 5s deadline waiting for a post-reinitialize event, confirming the dead pipeline; post-fix: passes in ~0.4s). Full ctest suite green (411/411); TSan clean on `AxonVexSystemTest` (46/46, zero reports). | `system.hpp` (`isShuttingDown_` declaration), `system.cpp` (`publishEvent`, `eventProcessingLoop`) (fixed) |
| C39 | MEDIUM | **FIXED.** Found during C38 review. `initialize()` assigned over `eventProcessingThread_`/`monitoringThread_` (`system.cpp`, near the thread-start calls) unconditionally; only `stop()`/`emergencyShutdown()` ever joined them, and `cleanupComponents()` didn't touch either. If `initialize()` threw after starting one or both threads, its catch path ran `cleanupComponents()` + `transitionState(ERROR)` and returned — leaving the `unique_ptr<std::thread>` members joinable. `ERROR` permits a transition to any state, so a retried `initialize()` assigned a new `std::thread` straight over the still-joinable old one; `unique_ptr<std::thread>::reset()`/assignment destroys the old thread object while it is still joinable, and `~thread()` on a joinable thread calls `std::terminate`. Same root cause had a second shape in `emergencyShutdown()`'s self-join guard (skips join+reset when called from the very thread it would join, deferring cleanup to a "second pass" not guaranteed to run before the next `initialize()`). **Reachability finding (this fix, not speculative):** the callback-initiated-e-stop → `reset()` → `initialize()` lifecycle (below) reliably reproduced `std::terminate` pre-fix (`Subprocess aborted`) — `reset()`'s own `emergencyShutdown()` call can `try_lock`-fail against the still-in-flight event-thread e-stop (`shutdownMutex_` contention) and return without joining, leaving the deferred-self-join handle stale for the next `initialize()`; the throw-mid-`initialize()` shape remains real but has no deterministic seam (allocation-failure-triggered). Fix: `AxonVexSystem::joinAndClearThreadHandle(std::unique_ptr<std::thread>&)` — joins (self-join guarded) and resets — called on both `monitoringThread_`/`eventProcessingThread_` immediately after `transitionState(INITIALIZING)` and *before* `initialize()`'s C38 flag writes (`isShuttingDown_.store(false)`, then the per-thread run flags). Ordering is load-bearing: at that point `isShuttingDown_` is still `true` and the run flags still `false` from the last shutdown, so any stale thread's loop condition is already guaranteed-exiting; clearing/setting those flags before the join would let a stale loop observe "revived" flags and never exit, turning the join into a potential indefinite block (the `currentState_ >= STOPPING` guard alone doesn't cover this window — `transitionState(INITIALIZING)` has already moved state off `ERROR`/`FATAL_ERROR` by then). Regression test `ReinitializeAfterCallbackInitiatedEmergencyShutdown` (pre-fix: `Subprocess aborted`/`terminate called without an active exception`; post-fix: green, 8/8 reruns, no flake). Verified 419/419 ctest, TSan clean on `AxonVexSystemTest` (47/47, zero reports). **Follow-up hardening (whole-branch review):** the joins were moved from mid-`initialize()` to the very top, above `initializeComponents()` — a stale thread from the exact same lifecycle could still be running inside `emergencyShutdown()`/the loops, dereferencing `logger_`/`timingController_`, at the moment `initializeComponents()` replaced those members; `reset()`'s `sleep_for(100ms)` only masked the window, it is not a synchronization point (see C40). `joinAndClearThreadHandle`'s self-case also unconditionally reset the handle after skipping the join, so `initialize()` called from a system-thread callback hit `~std::thread()` on a still-joinable self-handle → `std::terminate`; the self case was patched to `detach()` instead. **Correction (C41 review):** the claim that the regression test below exercises that self/detach branch was false — `ReinitializeAfterCallbackInitiatedEmergencyShutdown`'s `initialize()` call runs on the test's main thread, not on a worker thread, so it only ever exercises the cross-thread join branch. The self/detach branch was reachable but untested; C41 (below) closes it by making `initialize()` refuse outright on a worker thread, removing the detach branch entirely rather than leaving it as unverified defense-in-depth. | `system.hpp` (`joinAndClearThreadHandle` declaration), `system.cpp` (`initialize()`, `joinAndClearThreadHandle` definition) (fixed) |
| C40 | MEDIUM | **FIXED.** Found during whole-branch review of C39. `stop()` called `eventProcessingThread_->join()`/`monitoringThread_->join()` directly (no self-join guard, unlike `emergencyShutdown()`'s C33/C36 guard or `joinAndClearThreadHandle`'s). A user event callback that called `stop()` on its own system ran on `eventProcessingThread_` and joined itself: `std::thread::join` on the calling thread throws `system_error(resource_deadlock_would_occur)`, caught by `stop()`'s own `catch (const std::exception&)`, logged as `"Stop failed: ..."`, and escalated to `emergencyShutdown()` — a graceful stop silently became `FATAL_ERROR` instead of `STOPPED`. Wrong outcome, not a crash: the state machine already blocks the concurrent-stop shape (`transitionState(STOPPING)` rejects `STOPPING → STOPPING`), so this was specifically the self-join case. Second shape of the same "worker thread outlives the objects it uses" family as C39: `reset()` used `std::this_thread::sleep_for(std::chrono::milliseconds(100))` after `emergencyShutdown()` as its only synchronization before tearing down `logger_`/`timingController_` — a fixed sleep is not a happens-before edge. Fix: `AxonVexSystem::stop()` and `AxonVexSystem::reset()` each reuse C41's `isOnWorkerThread()` and refuse (log an ERROR naming `emergencyShutdown()` as the alternative, then return `false`/`void`) as their first meaningful action — `stop()`'s check runs before `transitionState(SystemState::STOPPING)` so a refused call leaves `currentState_` untouched instead of escalating; `reset()`'s check is its literal first statement. This removes the self-join shape entirely rather than guarding it, and closes the deadlock risk a *blocking* wait would otherwise carry: `reset()`'s `sleep_for` is replaced with `{ std::lock_guard<std::mutex> wait(shutdownMutex_); }` (the destructor's existing idiom) right after `emergencyShutdown()` — a real synchronization point for a *mutex-owning* in-flight teardown, safe against deadlock precisely because a worker thread can never reach it (the refusal above catches it first), so nothing joining *us* can be blocked holding this mutex. **Correction (whole-branch review):** the wait alone does not cover a *deferred-self-join* `emergencyShutdown()` — one running ON `eventProcessingThread_`/`monitoringThread_` itself, which `try_lock`-skips the mutex and returns while still unwinding user-code frames on that thread; `reset()` and `~AxonVexSystem()` both now call `joinAndClearThreadHandle(eventProcessingThread_)`/`joinAndClearThreadHandle(monitoringThread_)` immediately after the wait to close that window (legal — no self-join risk, since a worker thread cannot reach either path). Regression tests `StopFromEventCallbackIsRefusedWithoutEscalation` (pre-fix: fails on `EXPECT_EQ(getState(), RUNNING)` — state lands on `FATAL_ERROR`; post-fix: green) and `ResetFromEventCallbackIsRefused` (pre-fix: `Subprocess aborted`/`terminate called without an active exception` — the callback's `attempted->exchange(true)` fires before `reset()` finishes tearing down, so the test's own concurrent `stop()` races the in-flight teardown; post-fix: green, since the refusal returns before any teardown starts, so no concurrent-lifecycle race exists to have). `ResetIsAValidatedStateTransition` (C7's reset() regression, coupled to the removed sleep) still discriminates: its `reset()` call runs on a plain `std::thread`, so `emergencyShutdown()` actually joins `eventProcessingThread_`/`monitoringThread_` there (not skipped via the self-join guard), and each loop only re-checks its shutdown flag on its own ~100ms poll cadence — that real join time, not a guessed delay, is the window the test's poll now observes; 30 consecutive reruns green, 0 flakes. Verified 422/422 ctest, TSan clean on `AxonVexSystemTest` (50/50, zero reports). | `system.hpp` (`stop()`, `reset()` doc comments), `system.cpp` (`stop()`, `reset()`) (fixed) |
| C41 | HIGH | **FIXED.** Parked at the C39/C40 breaker. Two shapes: (1) the latent hazard — `initialize()` called from an event callback runs on `eventProcessingThread_` itself; `joinAndClearThreadHandle`'s old self-case `detach()`ed the self-handle instead of joining it, and `initializeComponents()` then replaced `eventPool_`/`eventQueue_`/`logger_`/`timingController_` out from under the still-live detached loop's own stack — when the callback returned, the old loop body resumed against freed/replaced objects: `eventPool_->deallocateObject(eventPtr)` freed a pointer from the DESTROYED pool into the NEW pool (heap-use-after-free, ASan-confirmed, 5/5 reproductions in `SystemEvent::~SystemEvent()` via `MemoryPool::deallocateObject`), and two consumers ended up draining one queue. Pre-C39 this shape was a loud `std::terminate`; C39's detach made it silent instead of fixing it. (2) the false coverage prose — the C39 row and the CHANGELOG C39-follow-up entry both claimed the regression test exercised the self/detach branch; it does not (`ReinitializeAfterCallbackInitiatedEmergencyShutdown`'s `initialize()` runs on the test's main thread, only ever hitting the cross-thread join branch) — corrected in place above and in `CHANGELOG.md`. Fix: `eventProcessingLoop()`/`monitoringLoop()` each publish their own `std::this_thread::get_id()` into a new `std::atomic<std::thread::id>` (`eventThreadId_`/`monitoringThreadId_`) as their first act via a small RAII guard whose destructor clears it back to `std::thread::id{}` on every exit path (ids are reusable once a thread exits — a stale id left behind could later alias an unrelated thread); `isOnWorkerThread()` compares `std::this_thread::get_id()` against those two atomics, never against a thread handle's `get_id()` (which C40 already flags as unreliable to read concurrently with teardown). `initialize()` calls `isOnWorkerThread()` as its literal first statement and refuses (logs an ERROR, returns `false`) before doing anything else, including the C39 joins. `joinAndClearThreadHandle`'s self/detach branch is now unreachable from its only caller and is **removed** — the tightened contract comment states callers must not run on the thread the handle names, guaranteed by `initialize()`'s refusal. Regression test `InitializeFromEventCallbackIsRefused`: pre-fix, 5/5 ASan runs aborted with heap-use-after-free (plus the assertion that `initialize()` wrongly returned `true`); post-fix green. Verified 420/420 ctest, ASan clean, TSan clean on `AxonVexSystemTest`. | `system.hpp` (`eventThreadId_`, `monitoringThreadId_`, `isOnWorkerThread()` declaration), `system.cpp` (`eventProcessingLoop()`, `monitoringLoop()`, `isOnWorkerThread()`, `initialize()`, `joinAndClearThreadHandle()`) (fixed) |
| C42 | MEDIUM | **FIXED.** Found during whole-branch review of C40/C41. `isOnWorkerThread()`'s refusal covered only `eventThreadId_`/`monitoringThreadId_` — not `RealTimeScheduler`'s own worker thread. A `ProcessingUnit` task body (or any callback the scheduler invokes on its worker, e.g. the error callback) calling `system.stop()`/`system.reset()` self-joined the scheduler thread inside `TimingController::stop()` -> `RealTimeScheduler::stop()`; the thrown `system_error` was caught and silently escalated to `emergencyShutdown()`/`FATAL_ERROR` — C40's exact symptom, reachable from the primary user extension point (`ProcessingUnit`/task callbacks) rather than only system event/health callbacks. Reproduced pre-fix: regression test `AxonVexSystemTest.StopFromProcessingUnitTaskIsRefusedWithoutEscalation` failed on `EXPECT_EQ(getState(), RUNNING)` (state landed on `FATAL_ERROR`), and a subsequent `stop()` call crashed the process (`terminate called without an active exception`). Fix: `RealTimeScheduler` gains `schedulerThreadId_` (`std::atomic<std::thread::id>`), published by `schedulerLoop()` as its first act and cleared as its last via a `WorkerThreadIdGuard` copied into `timingController.cpp`'s anonymous namespace (same shape as `system.cpp`'s C41 guard — not shared across translation units, no ODR conflict since both are internal-linkage). `RealTimeScheduler::stop()` checks `isOnSchedulerThread()` before touching `schedulerThread_`; on a self-call it stops the loop (`running_.store(false)`) and *defers* the join and handle reset — joining would still be a self-join (C33/C36 shape, `std::terminate`). `stop()` also lost its `if (!running_.load()) return;` early guard (SafetyManager/Watchdog C33/C36 precedent: after a deferred self-stop the flag is already false while the handle still needs reclaiming, so every step must be independently idempotent) — the deferred handle is absorbed by the next `stop()` call from a real thread, or by `~RealTimeScheduler()`. The destructor now calls `stop()` unconditionally and, if `schedulerThread_` is *still* joinable afterward (only possible if the destructor itself is running on the scheduler thread — the same undestructible contract violation documented on `~AxonVexSystem` below, inherited through `TimingController`'s owning `unique_ptr`), `detach()`s rather than joins it (`SafetyManager::~SafetyManager()`'s exact precedent). `TimingController::isOnSchedulerThread()` forwards null-safely; `AxonVexSystem::isOnWorkerThread()` now also checks `timingController_ && timingController_->isOnSchedulerThread()` (aliasing argument documented at the call site: every caller either checks on lifecycle entry before mutating any component, or is a scheduler-thread task/callback for which `timingController_` being alive is implied by the call itself). New destructor lifetime contract (parked item, documented not code-enforced — a destructor cannot refuse): `~AxonVexSystem` must never run on a system worker thread (event/monitoring/scheduler) — if the last owning reference is released inside a worker-thread callback, the destructor's `joinAndClearThreadHandle()` self-joins, which is undefined behavior no refusal can catch. Verified 428/428 ctest, TSan clean on `AxonVexSystemTest`\|`TimingController` (78/78, zero reports), 6 concurrent processes × 6 repeats of the full `AxonVexSystemTest`/`TimingControllerTest` suites (36 runs × 78 tests) all green. **Review follow-up (same wave):** `RealTimeScheduler::start()` was untouched by the fix above and had the identical hazard one layer down — after a deferred self-stop, `running_` is false but `schedulerThread_` is still joinable; any thread calling `start()` next hit `unique_ptr::operator=` destroying a joinable `std::thread` (C39's exact shape, uncatchable `std::terminate`, no log). `start()` now reads `isOnSchedulerThread()` before touching `schedulerThread_` at all: an external-thread restart joins the leftover handle first (mirrors `joinAndClearThreadHandle`'s shape); a restart called from the scheduler thread itself (still inside the deferring call's own stack, which has not unwound out of `schedulerLoop()` yet) refuses outright rather than detach — detaching the still-live old loop would recreate the C41 hazard. Regression test `TimingControllerTest.StartAfterDeferredSelfStopReclaimsHandleAndRunsAgain` (pre-fix: uncaught `std::terminate`; post-fix: green, and the scheduler is confirmed actually running again, not just reporting `isRunning()`). This closes the same root the `docs/v1_release_plan.md` C42 note about raw `TimingController::reset()`/`emergencyStop()` from a task already flagged: any of `RealTimeScheduler`'s public entry points reached directly (bypassing `AxonVexSystem`'s refusal chain) share this one guard now that `start()` has it too. Verified 429/429 ctest, TSan clean on `AxonVexSystemTest`\|`TimingController` (79/79), 6×6 parallel-load gate (36 runs × 79 tests) all green. | `timingController.hpp`/`.cpp` (`RealTimeScheduler::schedulerThreadId_`/`isOnSchedulerThread()`/`start()`/`stop()`/`~RealTimeScheduler()`, `TimingController::isOnSchedulerThread()`), `system.cpp` (`isOnWorkerThread()`), `system.hpp` (`~AxonVexSystem` lifetime-contract warning, `initialize()`/`stop()`/`reset()` doc updates) (fixed) |
| C43 | HIGH | **FIXED** (2026-08-07, commits 47bf938/fa27c02). Found by the Phase-2 RT-path audit (2026-08-06). `scheduleCustom` invokes the user-supplied `customScheduler_` callback **while holding `tasksMutex_`** — a live instance of the C12/C18 user-code-under-a-lock class, on the scheduler hot path: a custom scheduler that calls `addTask`/`removeTask`/`getStatistics` self-deadlocks. It also heap-copies the entire task table (including each `std::string name`) into a `std::vector<SchedulerTask>` every cycle, violating the RT alloc-free invariant. **Resolution decided 2026-08-07 (design-spec Q6): FIX, do not delete.** Deletion was proposed on the grounds that `SchedulingPolicy::CUSTOM` cannot honor the RT invariant even when fixed (user code on the scheduler thread every tick) and has no in-tree user; the policy is kept instead. Fixed by snapshot-under-lock → release → invoke: `tasks_` is deep-copied into a reusable member buffer (`customSchedulerSnapshot_`) under `tasksMutex_`, the lock is released, and only then is `customScheduler_` invoked — so the snapshot owns what it hands the callback and no user code runs under the lock. A stale returned id (the callback may legally mutate the task set once the lock is released) is absorbed by the same find-under-lock-before-use pattern `schedulerLoop()` already applies to every scheduling algorithm's selection, just with a wider window; documented at the site. Allocation ceiling, named in a header comment: the reusable buffer amortizes to zero allocations once the task set and its names stabilize, but a task-set change still allocates on the next cycle — a pool-backed buffer is the stated upgrade path, deliberately not built for an opt-in policy that cannot honor the RT invariant anyway. Regression test `CustomSchedulerCallbackCanReenterSchedulerApi` (a callback re-entering `getActiveTaskIds()`) self-deadlocked pre-fix and is deadline-guarded because its RED mode is a hang; it deliberately leaks the controller on the timeout path, since `~TimingController` would join the permanently-blocked scheduler thread and wedge the suite. | `timingController.cpp` (`RealTimeScheduler::scheduleCustom`) |
| C44 | MEDIUM | **FIXED** (2026-08-07, commits 47bf938/fa27c02). Found by the Phase-2 RT-path audit (2026-08-06). `errorCallback_` (a `std::function`) is read unlocked on the scheduler thread in `executeTask`'s failure path while `setErrorCallback` writes it under `schedulerMutex_` — an unsynchronized read of a non-atomic `std::function` is a torn read (the target pointer and the captured-state pointer are not updated atomically), so a `setErrorCallback` concurrent with a task failure can dispatch through a half-written object. Fixed per the C32 precedent: a one-way `hasStarted_` latch (set at the top of `start()`, never cleared) plus `requireNotStarted()`, mirroring `BasePort::requireNoTrafficYet`. `setErrorCallback` now throws `std::logic_error` once the scheduler has ever started, which makes the unlocked read on the RT path legal rather than putting a lock there. Blast radius verified before the contract change: the only real in-tree caller is `AxonVexSystem::initializeComponents()`, which registers on a freshly-constructed `TimingController` before `startComponents()` calls `start()`. **Sibling swept in the same wave (commit fa27c02):** `customScheduler_` had the identical shape — written under `schedulerMutex_` by `setCustomScheduler`, read unlocked at TWO points in `scheduleCustom` (the null guard and the invocation), which is both a torn read and a check-then-act window where the callback can be cleared between the check and the call. The first fix pass assessed it as "moot"; direct code reading proved it was not, and it now shares the same setup-only latch. All three in-tree callers (`timing_controller_example.cpp`, two tests) already register before `start()`. Residual, documented and not repairable by a runtime check: a `setCustomScheduler`/`setErrorCallback` racing the very first `start()` from another thread can slip past the latch — that is a caller-contract violation (register before starting, from one thread). |  `timingController.cpp`/`.hpp` (`RealTimeScheduler::executeTask`, `setErrorCallback`) |
| C45 | MEDIUM | **FIXED** (2026-08-07, commit 47bf938). Found by the Phase-2 RT-path audit (2026-08-06). `scheduleRoundRobin` keeps its rotation cursor in a **function-local `static uint32_t lastSelectedTask`** — one variable shared by every `RealTimeScheduler` instance in the process. Two systems running concurrently corrupt each other's round-robin ordering (tasks skipped or repeated, silently and non-deterministically), and two scheduler threads reading/writing it is also a plain data race. Fixed by moving it to a per-instance member (`lastSelectedTask_`) under the `tasksMutex_` the function already holds; a sweep of the file found no other function-local-static of this shape. **Testing lesson worth carrying:** the first regression test — two controllers, real threads, disjoint task-id ranges, comparing execution balance — **passed against the unfixed code**, because `scheduleRoundRobin`'s "scan up to `tasks_.size()` candidates" loop compensates for a corrupted cursor when only two always-ready tasks exist (both get visited within one pass regardless of start position). The replacement drops threads entirely: a test-only friend accessor calls `scheduleRoundRobin()` directly and compares an isolated instance's six-call selection sequence against the same construction interleaved with a second, disjoint instance — pre-fix `{3,3,3,3,3,3}` (foreign cursor values force a permanent reset to `begin()`) versus the `{3,2,1,3,2,1}` baseline. A green test against unfixed code is evidence about the test, not the code. | `timingController.cpp` (`RealTimeScheduler::scheduleRoundRobin`) |
| C46 | CRITICAL | **FIXED** (2026-08-07, Phase 2 wave 1). `TcpClient::setErrorCallback()` and `UdpSocket::setErrorCallback()` deadlocked on their very first call, 100% reproducibly: each took a `unique_lock` on `cbMutex_` and then called the virtual `registerErrorHandler()` override, which locks the same **non-recursive** mutex again. A mandatory `ProtocolInterface` entry point hung forever. It survived every green test run because the API had **zero test coverage anywhere in the tree** — a reminder that "N/N passed" bounds only what is tested. Fix: call the non-locking `errorKeyed_.registerKeyedCallback()` primitive directly, exactly as `setMessageCallback` already did. Regression tests `TransportCallbackApiTest.{Tcp,Udp}SetErrorCallbackRegistersWithoutDeadlockAndFires` assert both registration and delivery, and carry their own deadline because the pre-fix failure mode is a hang, not an assertion (proven RED pre-fix: both hit the 8 s deadline). The same shape exists in the `WebSocketServer` placeholder, which is now compile-gated by the §7 pass and therefore unreachable. | `axonvex_net/src/tcpClient.cpp`, `udpSocket.cpp` (`setErrorCallback`) (fixed) |
| C47 | HIGH | **FIXED** (2026-08-07, Phase 2 wave 1). `UdpSocket::recvLoop()` left `running_` true when it broke out on a hard `recvfrom` error, and `start()`'s `if (running_.load()) return true;` had no reap path — so a restart attempt **returned `true` while doing nothing** and the receive path stayed permanently dead. This is the project's signature silent-failure class (a platform/error path reporting success), sitting one file over from the TCP dead-connection fix landed in the same branch. Recovery required an explicit `stop()` first, which nothing documented. Fix: mirror TCP's `connectionAlive_` discipline — `recvLoop` marks the connection dead on every exit, `isRunning()` reflects both flags, `start()` refuses when called from its own worker thread and reaps a joinable-but-unjoined worker before reassigning `worker_`. Regression test `UdpConnectionLifecycleTest.RestartAfterSelfStopReapsDeadWorker` crashed the process pre-fix (`terminate called without an active exception` — assignment over a joinable `std::thread`, the C39 shape). Also fixed alongside: `UdpSocket::send()` returned a silent `false` when unopened, and the newly-loud `reportError()` created an unbounded `reportError → handler → send → reportError` recursion, now bounded by a thread-local in-dispatch guard. | `axonvex_net/src/udpSocket.cpp` (`recvLoop`, `start`, `send`, `reportError`) (fixed) |
| C48 | HIGH | **FIXED** (found 2026-08-07, the first time Conan was actually installed and the documented build path could be run). **The project's own canonical build sequence does not work.** `conan install . --output-folder=build --build=missing` then configuring with `conan_toolchain.cmake` configures cleanly (exit 0) and then fails to link with **14,373 undefined GTest references** (`testing::Test::SetUp()`, `typeinfo for testing::Test`, and `undefined reference to 'main'` — neither gtest nor gtest_main is linked); `test_core` and `test_ros2_adapter` both fail, and ctest reports 0% passed, 4/4 failed. **Root cause (first hypothesis was WRONG and is recorded here deliberately):** the initial diagnosis blamed a legacy-vs-modern target-name mismatch (`GTest::GTest`/`GTest::Main` versus `GTest::gtest`/`GTest::gtest_main`). That is false — inspection of the generated `GTestTargets.cmake` shows Conan emits the legacy names too, as aliases forwarding to the modern ones, and **both are equally broken**, so renaming would have fixed nothing. The real mechanism, confirmed in `GTest-Target-release.cmake`: CMakeDeps wraps every real property (`INTERFACE_LINK_LIBRARIES`, `INTERFACE_INCLUDE_DIRECTORIES`, `INTERFACE_LINK_DIRECTORIES`) in a `$<$<CONFIG:Release>:...>` generator expression, because `conan install` with no `-s build_type=` uses the default profile's `build_type=Release` — while the documented next step configures `-DCMAKE_BUILD_TYPE=Debug`. On a single-config generator every one of those conditionals evaluates false, so the imported targets exist carrying **zero** libraries, include dirs and link dirs; `link.txt` contains no `-lgtest` at all. Configure therefore passes and the failure is deferred to link time. **The same gating silently affects nlohmann_json and TBB**: `axonvex_core.so`'s link line contained no `-ltbb`, and the shared-library link still "succeeded" because shared objects tolerate undefined symbols by default — a latent break that would surface only when a real TBB symbol is needed. It went unnoticed because the tree is always built here against system packages, where it is 559/559 green: **the documented path and the exercised path were never the same path.** Second defect found in the same place: `find_package(GTest QUIET)` + `else() message(STATUS "GTest not found; skipping tests.")` means a missing GTest silently deletes the entire test suite while the build still reports success — a CI job could pass having compiled nothing testable, which is the project's own banned silent-success class. **FIXED** (commits 2d7e863/6d90b76): GTest link targets resolve once (modern preferred, legacy fallback, `FATAL_ERROR` if neither exists); the build_type mismatch is now detected at **configure** time by checking `GTest_DIR` for a `GTest-<config>-*-data.cmake` matching `CMAKE_BUILD_TYPE`, failing with the exact `conan install ... -s build_type=<X>` command instead of a 14k-line linker wall; and `BUILD_TESTING=ON` with no GTest is now a `FATAL_ERROR` rather than a `message(STATUS "...skipping tests")` that let the build report success while shipping zero tests. Verified 559/559 on **both** paths (conan toolchain with aligned build_type, and system packages), zero warnings each. Remaining narrow gap, documented not fixed: `-DBUILD_TESTING=OFF` with a mismatched build_type still surfaces as a confusing `nlohmann/json.hpp: No such file` rather than a clear message, because `add_subdirectory(src/libs)` runs before the new guard. **Two standing lessons: (1) verify the build path you document, not just the one you happen to run — the documented and exercised paths were never the same path here, which is why 559 green tests coexisted with a completely broken build; (2) a confident root-cause hypothesis is not a diagnosis. The first explanation for this failure was wrong, and only reading the generated CMake files found the real one.** | root `CMakeLists.txt` (`find_package(GTest QUIET)`, `GTest::GTest`/`GTest::Main` link sites) |
| C51 | MEDIUM | **FIXED** (2026-08-07). Found by the newly-real clang-tidy gate (`concurrency-mt-unsafe`), which until this week ran with no config and therefore passed by checking nothing. `strerror(errno)` was called at 5 sites in `axonvex_net`'s TCP and UDP error paths. POSIX does not require `strerror` to be reentrant — it may format into a shared static buffer — and these paths run on each connection's own recv/send thread, so two transports erroring concurrently can interleave inside it. **This is the same defect class as C37** (non-reentrant `std::localtime` in the logger, found only when a new test first ran two `Logger`s concurrently under TSan). Fixed by replacing all 5 with `std::generic_category().message(errno)`, which libstdc++ implements over `strerror_r`. Verified by before/after clang-tidy runs showing exactly those 5 diagnostics removed and no others introduced. Deliberately left deferred and tracked rather than silently ignored: `path.cpp`'s `getenv`/`getpwuid` hits (single-shot init-time home-directory lookups, not per-connection paths) and `pluginManager.hpp`'s `strerror`. **Lesson: a quality gate that passes by doing nothing hides real defects for as long as it exists.** | `axonvex_net/src/tcpClient.cpp`, `udpSocket.cpp` (error-reporting paths) (fixed) |
| C50 | HIGH | **OPEN** (found 2026-08-07 while verifying C49). `tests/core/builtinUnitsTest.cpp` installs a **process-wide** `operator new(size_t)` / `operator delete` override backed by `malloc`/`free` (to count allocations for the RT-path assertion on `MovingAverage`), but does not provide the `nothrow` overloads. A global allocator override in one test file applies to the ENTIRE `test_core` process — all 559 tests — not just its own, so any allocation that goes through `operator new(nothrow)` is served by the real allocator and then freed through this file's `free()`. Manifestation is configuration-dependent, which is what makes it dangerous: with **system** GTest it is invisible (I measured ASan 550/550 green, zero mismatches, on the merged tree), while with **Conan-provided** GTest an agent reproduced 549/559 failures with `AddressSanitizer: alloc-dealloc-mismatch (operator new vs free)` — reportedly reached via `std::get_temporary_buffer` inside GTest's own `stable_sort`, which runs on every process invocation regardless of `--gtest_filter`. Fix direction: give the override the complete overload set (including `nothrow` and sized/aligned forms) or, better, replace the global override with a scoped counting allocator injected into the unit under test — a test should not be able to change allocation semantics for every other test in the binary. | `tests/core/builtinUnitsTest.cpp` (global `operator new`/`operator delete`) |
| C49 | CRITICAL | **FIXED** (commit b5a8a39, merged; found 2026-08-07 by the Phase-3 benchmark review; independently reproduced). **AxonVex cannot be built optimized, and the cache reports that it can.** Root `CMakeLists.txt:6` is a plain, non-`CACHE` `set(CMAKE_BUILD_TYPE Debug)`, which creates a directory-scope normal variable shadowing the cache variable — so `-DCMAKE_BUILD_TYPE=Release` on the command line is silently ignored for the whole build, while line 5's `set(CMAKE_CXX_FLAGS_DEBUG "-g -O0")` pins it to no optimization. Reproduced: `cmake -B probe -DCMAKE_BUILD_TYPE=Release` yields `CMAKE_BUILD_TYPE:STRING=Release` in `CMakeCache.txt` and `CXX_FLAGS = -g -O0 -std=gnu++14 -fPIC` in `flags.make`. **Every build in this project's history has been `-O0`**, and every tool that inspects the cache reports the opposite of the truth — which is why it survived this long. Consequences: (1) the README's performance claims (1M+ msgs/sec, sub-µs timing class) have never been measured in an optimized build; (2) the new Phase-3 benchmark harness produces `-O0` numbers under docs that label them Release — mislabeled measurements that get quoted downstream forever, the fabricated-measurement risk realized structurally rather than by anyone inventing a figure; (3) it is the deeper cause behind **C48** — Conan's default profile installs Release packages and CMakeDeps gates every property behind `$<$<CONFIG:Release>:...>`, so with the effective config forced to Debug those properties evaluate empty and imported targets carry zero libraries (the benchmark track hit this as `fatal error: benchmark/benchmark.h: No such file or directory` *after* a successful configure). **Fixed** with guarded CACHE sets: an explicit `-DCMAKE_BUILD_TYPE` now wins, Debug remains the default when nothing is passed, multi-config generators are left alone, and `CMAKE_CXX_FLAGS_DEBUG` is only seeded when no cache entry exists (so a user's own value survives reconfigure). Verified on the merged tree: `-DCMAKE_BUILD_TYPE=Release` → `CXX_FLAGS = -O3 -DNDEBUG -std=gnu++14 -fPIC`; no flag → `-g -O0`. Debug suite unchanged at 559/559; ASan still pinned to Debug (the sanitizer path passes no `CMAKE_BUILD_TYPE`, so it lands on the default exactly as before — confirmed, since silently optimizing sanitizer builds would hide the bugs they exist to find); the previously-impossible Conan-Release combination now configures and builds clean. **What the first-ever `-O3` run exposed, which was the point of the exercise:** (1) `AxonVexSystemTest.ReinitializeAfterCallbackInitiatedEmergencyShutdown` **hung for 900s+** (main thread blocked on a futex, one thread in `hrtimer_nanosleep`) and then passed in isolation on retry — this is the *same* test that hung once under the wave-2 parallel-load gate and could not be reproduced then, so Release timing is a second, independent sighting of one real lost-wakeup/self-join race, not two flakes; it is a genuine open bug on the lifecycle path the remaining decomposition steps must touch. (2) `TimingControllerTest.SchedulerPauseAndResume` fails inside the aggregate run at `-O3` (`countDuringPause 20 vs 18`) while passing in isolation — a real timing-margin defect, tighter under optimization. Neither was retuned away. Also fixed in passing: a `-Wstringop-overread` that only appears at `-O3` in `tests/net/transportTestUtils.hpp` (GCC 11 libstdc++ false positive on a reallocating `vector::insert`), closed with a `reserve()`. | root `CMakeLists.txt:5-6` |

**Status (2026-08-05):** C4, C10, C16, C17, C23 **fixed** with regression tests (C4/C16/C17; C10/C23 not observably testable until Phase 4 loopback tests). C18 **fixed** with regression test (`HealthCheckCallbackReentrantRegistration`): root cause was proven from a core dump — the monitoring thread invoked a health callback late (blocked on `callbacksMutex_` behind the test thread, after an unsynchronized epoch read of `lastHealth_.lastCheckTime` triggered a spurious immediate check), and the test's dangling `bool&` capture wrote `0x01` over a return address in the main thread's reused stack. Fix: snapshot-then-dispatch for health/event callbacks, loop-local health-check timer (`lastHealth_` deleted), scheduler error callback + `std::cerr` moved outside `statsMutex_`, test capture by `shared_ptr`. Verified: 374/374 ctest green, 0/100 crashes on the old repro (was 12/30), ASan clean (pre-existing C25 leak aside), TSan clean at C18's sites (remaining races are C25). C18's `executeTask` path still runs callbacks under the *caller's* `tasksMutex_` — that hold is C1's scheduler-loop fix. C1 **fixed** with regression tests (`AllReadyTasksExecutePerCycle` — pre-fix, 2 of 3 same-priority tasks got 0 executions in 600 ms; `RemoveTaskDuringExecutionIsSafe`): the scheduler loop now claims each ready task under `tasksMutex_`, executes without the lock, and releases/deallocates under the lock; `removeTask`/`removeAllTasks` defer deallocation of executing tasks via `SchedulerTask::pendingRemoval`; `executeTask`'s task bookkeeping (constraints/nextExecution/lastExecution) re-acquires `tasksMutex_` so only the user code itself runs unlocked (else it races `updateTaskConstraints`). Verified 376/376, ASan clean, TSan 0 warnings on scheduler tests. C2 **fixed** with regression tests (`SystemSafetyTest`): new core-owned `core::SafetyHook` interface (isEmergencyStopped/setEmergencyCallback) replaces the dead `setSafetyManager` API and removes the `axonvex::safety` forward-decl layering violation from `system.hpp`; `SafetyManager` implements the hook and fires the callback on e-stop (snapshot-then-dispatch); `AxonVexSystem::setSafetyHook` wires it to `emergencyShutdown`. Teardown hardening from review: hook dispatch runs under `emergencyCallbackMutex_` so `~AxonVexSystem`'s automatic `setEmergencyCallback(nullptr)` synchronizes with in-flight dispatch (no dangling `this`); `shutdownMutex_` (try-lock in `emergencyShutdown`, blocking in `stop()`'s join section and the destructor's wait) serializes thread-handle teardown now that e-stop can fire from any thread; self-join guards + destructor second-pass join cover e-stop from a system thread (regression test `EmergencyStopFromSystemThreadIsSafe`). Note: `evaluateAll` still calls `triggerEmergencyStop` under `policiesMutex_` — that is C12's scope. Verified 379/379, ASan clean, TSan clean at C2 sites (only the catalogued C25 warnings remain). C3 **fixed**: tagged free-list head (`{tag:32, index:32}` in one 64-bit atomic, tag incremented on every pop/push — memory-ordering argument documented in `memoryPool.hpp`); stress regression test `ConcurrentAllocateDeallocateNoDoubleHandout` is TSan/ASan-clean (the pre-fix ABA window was not natively reproducible; the test is the standing guard). Known: `MemoryPoolTest.PerformanceTest` asserts >10M ops/s and fails under TSan's slowdown by design — exclude perf tests from sanitizer CI jobs when presets land. C25 **fixed**: `updateSystemConfiguration` rejected once initialization begins (config immutable while worker threads read it — TSan clean, was 2 races); event-string leaks closed via shutdown-time publish guard + `drainEventQueue()` after event-thread join and before `initialize()`'s component rebuild (LSan zero leaks on system+safety tests, was 2072 B / 37 allocs). C12 **fixed** with regression tests (`HandlerMayMutatePolicyRegistryDuringDispatch`, `PolicyMayQueryManagerDuringEvaluate` — both deadlocked pre-fix; `EvaluationPeriodMayChangeWhileRunning`): `evaluateAll()` snapshots `{name, shared_ptr<SafetyPolicy>}` under `policiesMutex_` and then runs `evaluate()`, event dispatch, and `triggerEmergencyStop` with no policy lock held (the `shared_ptr` keeps a policy removed mid-cycle alive until the cycle ends); a new `evaluationMutex_` serializes evaluation cycles so a policy is never re-entered concurrently, and the thread running a cycle is published in `evaluatingThread_` so a re-entrant `evaluateAll()` from a policy/handler returns the cycle's worst-so-far level instead of blocking on a mutex it already holds or recursing until the stack is gone (`ReentrantEvaluateAllFromHandlerReturnsInsteadOfHanging`, `EmergencyStopFromHandlerDoesNotHang` — the first hung before the guard, found by cpp-reviewer); handlers moved off the entirely unsynchronized `core::Caller` to a `handlersMutex_`-guarded vector with snapshot-then-dispatch, so cross-thread and re-entrant (un)registration are safe; `evaluationPeriod_` is now `std::atomic<Duration>` (the evaluation loop read it unlocked while setters wrote it under `policiesMutex_`). Verified 385/385 ctest, TSan and ASan clean across the safety suite. Two load-sensitive tests (`TimingControllerTest.SchedulerPauseAndResume`, `PrecisionTimerTest.PerformanceTest`) fail intermittently when the machine is busy and pass on rerun — both are sleep/throughput-based and belong in the Phase-1 flake cleanup. Note for the sanitizer CI work: GCC 11's libtsan does not intercept `pthread_cond_clockwait`, so `condition_variable::wait_for` in test helpers produces bogus "double lock of a mutex"/race reports — poll an atomic with a deadline instead. Next up: sanitizer CI presets, then C11/C13/C14. C5, C7, C9 **fixed** (see their rows above). C37 (Logger::defaultFormat non-reentrant `std::localtime` data race, found during C9 verification) is **fixed**: `localtime_r`/`localtime_s` replace `std::localtime`, TSan-verified clean (45/45 `AxonVexSystemTest` across 3 runs, previously-failing test green 4/4 direct runs with 0 TSan report lines, was exit 66 / 2 warnings reproduced 4/4 pre-fix). C38 (`isShuttingDown_` never cleared, so a `reset()` + `initialize()` cycle left the re-initialized system's event thread exiting immediately and `publishEvent` silently dropping everything — found during C37 review) is **fixed**: `initialize()` is now the single owner of clearing the flag, verified by regression test `EventPipelineIsAliveAfterResetAndReinitialize` (411/411 ctest, TSan clean on `AxonVexSystemTest`, 46/46). C6 (declared-but-unimplemented public API on `AxonVexSystem`) is **fixed**: the five dead methods are deleted, not stubbed. C8 (`ProcessingUnit`'s advertised-but-compiler-deleted move ops) is **fixed**: the move ops are now explicit `= delete` with static_assert coverage. C39 (found during C38 review: `initialize()` can assign a fresh `std::thread` over a still-joinable `eventProcessingThread_`/`monitoringThread_` if a prior `initialize()` threw after starting one, since neither `cleanupComponents()` nor the `ERROR`-state retry path joins them first — `std::terminate` on the next `initialize()`; `emergencyShutdown()`'s self-join guard has the same root cause in a second shape) is **fixed**: `joinAndClearThreadHandle()` helper joins-and-clears both thread handles before `initialize()`'s C38 flag writes, ordering chosen so a stale thread's already-guaranteed exit isn't undone by the flag reset; verified by regression test `ReinitializeAfterCallbackInitiatedEmergencyShutdown` (reliably `std::terminate`d pre-fix via the callback-e-stop → `reset()` → `initialize()` lifecycle, green post-fix), 419/419 ctest, TSan clean on `AxonVexSystemTest` (47/47). C14 (`Logger::formatString` silently dropped every `logf`/`LOGF_*` argument; `LOGF_CRITICAL` was a malformed double-`logf` expansion that failed to compile at any use site) is **fixed**: see its row above for the `formatImpl` substitution mechanism, the macro repair, and the honest-perf-wording change to the class doc comment. C19 (`PrecisionTimer`'s O(n) `erase(begin())` eviction under `samples_mutex_`, non-steady `high_resolution_clock`, and throwing elapsed getters) is **fixed**: see its row above for the ring-buffer/`steady_clock`/zero-return details, including the elapsed-getter contract change. C39 picked up follow-up hardening in whole-branch review (join moved above `initializeComponents()`, self-case `detach()` instead of an unconditional `reset()`) — see its row above. **C40 is FIXED**: `stop()`/`reset()` reuse C41's `isOnWorkerThread()` to refuse outright when called from a system worker thread — `stop()` before its `STOPPING` transition (so a refused call leaves state untouched instead of self-joining into a swallowed exception and an `emergencyShutdown()`/`FATAL_ERROR` escalation), `reset()` as its first statement (so it never reaches the destructive teardown a worker's own callback stack could still be using). `reset()`'s `sleep_for(100ms)` timing guess is replaced with a blocking wait on `shutdownMutex_` — a real synchronization point for a mutex-owning in-flight teardown, safe from deadlock because the refusal above means no worker thread can ever be the one holding it against us; it does not by itself cover a deferred-self-join teardown, which `reset()`/the destructor now additionally close with a post-wait join of both thread handles (see the C40 row's correction note). See its row above for the regression tests and the coupled `ResetIsAValidatedStateTransition` rework. **C41 is FIXED**: the reinit-from-event-callback shape C39's detach made silent (a cross-pool free/duplicate queue consumer, ASan-confirmed 5/5) is closed by making `initialize()` refuse outright on a worker thread — `eventProcessingLoop()`/`monitoringLoop()` self-publish their thread id, `isOnWorkerThread()` checks it, `joinAndClearThreadHandle`'s now-unreachable self/detach branch is deleted rather than kept as untested defense-in-depth; also corrects the C39 row's and CHANGELOG's false claim that the existing regression test exercised that self/detach branch (it exercises only the cross-thread join branch — see the C39 row's correction note above). Verified 420/420 ctest, ASan clean, TSan clean on `AxonVexSystemTest`. **C21 is FIXED**: `RingBuffer`'s ctor no longer placement-news over the already-value-initialized result of `make_unique<T[]>` (was constructing every slot twice, destroying once — a leak for non-trivial `T`), and the class doc now states the honest SPSC-only contract with a memory-ordering argument comment next to `write_index_`/`read_index_`. See its row above for the regression test and the new SPSC stress test. Verified 424/424 ctest, ASan/LSan clean, TSan clean on `RingBufferTest` (3 reruns, zero report lines). **C42 is FIXED**: `RealTimeScheduler` now has the C36 id-publish/self-join discipline `eventProcessingLoop()`/`monitoringLoop()` already had — `schedulerThreadId_` + a copied `WorkerThreadIdGuard`, a self-join guard in `stop()` that defers the join instead of self-joining, and `AxonVexSystem::isOnWorkerThread()` extended to cover the scheduler thread, so `stop()`/`initialize()`/`reset()` refuse a `ProcessingUnit` task exactly like an event/monitoring callback. `~RealTimeScheduler()` gained a joinable-then-join-or-detach decision (`SafetyManager` precedent) for the case where the destructor itself runs on the scheduler thread. See its row above for the full account, the regression tests (`AxonVexSystemTest.StopFromProcessingUnitTaskIsRefusedWithoutEscalation`, `TimingControllerTest.StopFromTaskBodyDefersJoinInsteadOfSelfJoining`), and the new `~AxonVexSystem` lifetime-contract `@warning` this wave also added (destructor cannot refuse a worker-thread call the way `initialize()`/`stop()`/`reset()` can — documented, not code-enforced). **C15 is FIXED**: `Path`'s four shapes — DCLP on a plain `initialized_` bool, `size_t` underflow in `createConfigPath`/`createLogPath`'s extension check, substring-based (not component-based) traversal detection, and `isInWhitelist()`'s unlocked iteration of `default_dirs_` — are all closed; see its row above for the `ensureInitialized()` magic-static mechanism, the grep evidence that `default_dirs_` has post-init writers (`setDefaultDir`/`resetDefaultDirs`) which is why the magic static's write and `isInWhitelist()`'s read both take `static_mutex_`, and the manual both-separators component split behind the traversal fix. The DCLP race itself has no deterministic in-suite repro (statics are already initialized by earlier tests); verified by inspection plus TSan-clean `PathTest` (`ThreadSafetyTest`'s 8-thread concurrent construction included). Verified 429/429 ctest, ASan clean on `PathTest`, TSan clean on `PathTest` aside from `PerformanceTest`'s pre-existing hardcoded-threshold-under-TSan-overhead failure (same class as `MemoryPoolTest.PerformanceTest`, noted above — not a regression). **C22 is FIXED**: `MissionPipeline`'s five racy members (`status_`, `currentElement_`, `elements_`, `transitions_`, `startElementName_`) are now guarded by one non-recursive `stateMutex_`; every control method snapshots the affected `MissionElement*` and its state flip under the lock, then invokes `onEnter`/`onExit`/`reset` (user code) with the lock released, so a hook re-entering the control API cannot deadlock; `processSync()`'s RT-path comment names the cost (an uncontended lock/unlock a few times per tick) and the upgrade path (atomics + SPSC command queue) if a benchmark ever shows contention; `addElement()` documents the non-owned, stable-reference ownership contract; and `startPipeline()` now validates the transition graph (start element plus every transition's from/to) before running, failing fast through `setError()`/`getLastError()` instead of wedging silently. See its row above for the pre-fix TSan race reports, the three regression tests, and the verification matrix (432/432 ctest, TSan/ASan clean, 36/36 on the parallel-load gate). **C13 is FIXED**: see its row above for the four shapes (no `.so` ever produced, no ABI handshake, dup-name leak, unspecified `dlopen` scope), the `AXONVEX_PLUGIN_ABI_VERSION` handshake, `PluginManager::lastError()`, the `~PluginManager()` gap closed in the same pass, and the breaking change (out-of-tree plugins must now export `axonvex_plugin_abi_version`). Verified 436/436 ctest, ASan/LSan clean (dup-name leak test included), TSan clean, UBSan clean, `nm -D` symbol evidence across three build configs. **C20 is FIXED**: see its row above for the pair-keyed `TypeCasterRegistry` (a two-value `type_index` hash combine, C++14 has no `std::hash<pair>`), `createServer`'s required `requestMapper` and the honest documented-not-added response handling (`ServerUnit` has no response path to map into), the real `registerDefaultCasters()`/`defaultCasters.hpp` gated on a genuine `find_package(std_msgs QUIET)`, and the four factories' `unique_ptr` returns. **Review pass (same day) found three Important issues, all fixed:** (1) `registerCaster` silently overwrote (and freed) an existing pair's caster, but `createSubscriber`/`createPublisher`'s rclcpp callbacks capture that caster's raw address for the life of the subscription/publisher — re-registering a pair already wired into a live callback would dangle it. Chose refusal over shared_ptr-snapshot semantics: `registerCaster` now throws `std::logic_error` on a duplicate pair, one sentence — this is the smaller diff and matches the codebase's existing refuse-the-unsound-state precedent (C31/C32) over adding reference-counted sharing for a case with no legitimate use (nothing needs to swap a caster after a unit is wired against it); `createServer` does not use the registry at all (its mapper is caller-supplied, not registry-owned), so it was never actually exposed to this hazard despite the initial finding's phrasing. New test `TypeCasterRegistryTest.ReregisteringAnExistingPairThrows`. (2) The new TSan-skip guard checked only the `AXONVEX_SANITIZER` cache variable, but this repo's own `build-tsan` was instrumented by setting `CMAKE_CXX_FLAGS` directly, so the cache variable reads empty there and the guard's `else()` branch registered `ros2_adapter_tests` anyway, exactly reproducing the double-lock. Fixed to check `CMAKE_CXX_FLAGS MATCHES "-fsanitize=thread"` too; confirmed via `setarch $(uname -m) -R ctest --test-dir build-tsan -N` that `ros2_adapter_tests` is no longer listed post-fix. Investigated the double-lock's attribution per the finding's own instruction: `ldd` on both `librclcpp.so` and `test_ros2_adapter` shows `libspdlog.so.1` reached only via ROS's own `librcl_logging_spdlog.so`, and the TSan stack has zero frames in any axonvex file — it is rclcpp's internal logging dependency, not our code, so it is quarantined (not filed as a new C43). (3) `ServerMapsRequestThroughToServerUnit` constructed a server and never sent a request, so a regression back to the original `T data{}` bug would still pass it. Rewritten as a real round trip: a genuine `rclcpp::Client<SetBool>` sends `data=true` (distinct from bool's default `false`), a `SingleThreadedExecutor` spins the future to completion, and the test asserts the `ServerUnit`'s sync output actually received `true` via `setOutputCallback` — this fails if the mapping regresses. Verified 449/449 ctest green, `TypeCasterRegistryTest`/`Ros2AdapterSmokeTest` ASan-clean, `TypeCasterRegistryTest` UBSan-clean, `Ros2AdapterSmokeTest`'s 5 logical tests TSan-clean (the one remaining finding is the quarantined vendor double-lock above, unchanged by these fixes), 0 warnings across all four build configs. **Final review wave (same day) found four more issues, all fixed:** (1, CRITICAL) the constructor's `registerDefaultCasters()` auto-call inverted the plan's opt-in design — an app that also registered `std::string<->std_msgs::msg::String` itself (the shipped `axonvex_bridge_app` demo) hit the finding-1-round `std::logic_error` refusal for a pair the constructor had *just* silently registered behind its back, uncaught in `main()` → `std::terminate` at startup, reproduced and then fixed end-to-end (built and ran `axonvex_bridge_app` against a sourced `/opt/ros/humble`, via a scratch install of this tree's own `axonvex` CMake package: aborted pre-fix, runs cleanly post-fix with zero source changes needed in the example itself, since it never called `registerDefaultCasters()` — only the ctor's behind-the-back call was the problem). Constructor no longer calls it; the method moved to `public`. (2, IMPORTANT) `createClient` had the identical silent-payload-drop shape `createServer` was fixed for in this row above, missed in the first pass: it built a default-constructed `RosSrv::Request` regardless of what the pipeline pushed in. Now requires a `requestMapper`, mirroring `createServer`'s contract exactly; its doc comment also states honestly that `ClientUnit::processAsync()` (see `interfaceUnits.hpp`) signals "finished" as soon as the mapped request is handed to rclcpp, never "the service replied" — a pre-existing `ClientUnit` limitation, unchanged by this fix, now documented instead of silently implied. (3, IMPORTANT) `createPublisher`'s `@return` claimed "no lifetime coupling back to this adapter", directly contradicted by its own `@warning` two lines below and by the publish callback's capture of a raw `TypeCaster*` into this adapter's own registry — corrected to state the same outlive-the-adapter contract `createSubscriber` documents. (4, MINOR) `PluginManager::loadPluginsFromDirectory()`'s three early `return false` paths (no loader, `opendir()` failure, no platform support) left `lastError_` empty; each now sets a reason. Also closed as MINOR items: `typeCasterRegistry.hpp`'s C++17 contracted `namespace axonvex::ros2 {` (a header compiled into the C++14 `test_core` binary) is now nested `namespace axonvex { namespace ros2 { ... } }`; `tests/core/missionTest.cpp`'s `OrderTrackingElement` gained a comment stating its missing `reset()` override is deliberate and load-bearing for `ResetQueuedBeforeDrainDoesNotExitNeverEnteredElement`. Verified: 456/456 ctest green (`build`), ASan-clean on the touched test suites (`build-asan`), 0 warnings across all touched build configs. **With this wave, the "closes the original C1–C42 inventory" claim below is genuinely true** — the first C20 pass had reintroduced a new startup-abort and left `createClient` in the exact silent-drop state C20 exists to close; both are now fixed and verified against real Humble headers, not just claimed. **C11's remaining framing half is now also FIXED** (magic+length-prefixed wire format replaces the newline delimiter; see its row above for the wire format, the loud-violation handling, and the regression tests) — **this closes the original C1–C42 inventory in full.**

## 4. Honesty audit (docs vs reality)

The README's "Key Features" block claims things that do not exist **yet**. Since all of them are v1.0 scope, the fix is not deletion but truthful status: each claim becomes a tracked commitment ("in development, targeted for v1.0") until its workstream ships it, at which point the claim points at code, tests, and benchmarks. What must still be fixed immediately: fictional community URLs/professional-services copy, the broken `CONTRIBUTING.md` link, the static build badge, and the bridge-app README documenting a nonexistent app.

| Claim | Reality |
|---|---|
| "<1 µs deterministic timing" | Never benchmarked; cv-based wait + mutexed ports + SCHED_OTHER fallback make it implausible as stated. CI validates the *fallback*, never the RT path. |
| "1M+ msgs/sec, sub-ms latency" | No benchmark exists in the tree. |
| "10,000+ processing blocks" | Soak test covers queue volume only, 2 tests, self-described "not micro-benchmarks". |
| "Angular 17+ dashboard, 3D scene, topology" | Zero frontend code. Backlog E07 all "Not Started". |
| "MFA, RBAC, end-to-end encryption" | Zero matches in source. SRS itself defers this (REQ-DEF-003). |
| "Hardware/software watchdogs" | Software-only `std::thread` timer. No /dev/watchdog. |
| "Windows/macOS support" | Sockets are Linux-only with silent no-op fallbacks; unproven claim. Static "Build Passing" badge, not a live one. |
| Community/forum/discord/docs URLs, Professional Services, 24/7 support | Fictional. `CONTRIBUTING.md` linked but absent. |
| Bridge app README | Documents an app (Imu pipeline, `Ros2AdapterNode`, `DataLoggerPU`) that does not exist in code; wrong build paths; unused package.xml deps. |

## 5. Architecture debt

1. **`AxonVexSystem` god class** — ~13 responsibilities (lifecycle FSM, config, logger, event pool/queue/thread, monitoring thread, health, recovery, stats, unit registry, system-port registry + cross-system connect, adapter registry, safety injection, memory accounting); 1,771-line .cpp, 914-line header, 130 graph edges, 0.02 cohesion. Decompose into: `LifecycleController`, `UnitRegistry`, `SystemPortRegistry`, `EventBus`, `HealthMonitor`; System becomes a façade.
2. **Layering inversion** — core's public header forward-declares `safety::SafetyManager` (`system.hpp:18`). Invert: safety subscribes to core, or introduce a core-owned `SafetyHook` interface that the safety lib implements.
3. **No RT-path invariant** — mutexes, heap allocation, string building, `std::cerr`, and user callbacks all occur on scheduler/port hot paths. Establish and enforce: *no locks, no allocation, no I/O, no user callbacks on the RT path*; everything defers to the event thread.
4. **Production socket code in header-only INTERFACE libs** — syscalls recompiled per-TU, never object-tested, OS headers leak into consumers. Move TCP/UDP (and future WS) into a compiled `axonvex_net` library.
5. **Header bloat** — `logger.hpp` 1,086 lines, `ports.hpp` 1,065, `missionPipeline.hpp` fully inline. Split declaration/implementation where templates allow.
6. **Statistics zoo** — 7+ `*Statistics` structs; CRTP `PerformanceStatisticsBase` adopted by only 3 and its CRTP parameter is unused. Pick one model.
7. **~1,500–2,000 LOC of deletable weight** (see §7).

## 6. v1.0 scope — the full vision, mapped to workstreams

v1.0 ships **everything the project vision commits to**. Positioning stays: "the deterministic C++ pipeline runtime with a wired safety envelope, replayable execution — and a live operations dashboard." Every vision pillar maps to a workstream (WS) below; nothing is deferred.

| Vision pillar (README/SRS) | Workstream | What "done" means in v1.0 |
|---|---|---|
| Precision execution, <1 µs class timing | WS-PERF | Reworked scheduler + documented RT deployment profile (SCHED_FIFO, mlockall, isolcpus); published p99 jitter benchmark on reference hardware, CI-gated |
| High performance: 1M+ msgs/sec, sub-ms latency | WS-PERF | Port/queue hot path lock- and alloc-free; throughput + latency benchmarks published and regression-gated |
| Scalable: single-node → distributed, 10,000+ blocks | WS-SCALE | 10k-block soak test in CI (nightly); system-to-system port bridging over hardened TCP/UDP transports for multi-node composition |
| Modular design, plugin system | WS-ECO | Real MODULE `.so` plugins with C ABI version handshake; ROS 2 plugin complete (casters, services); MAVLink adapter functional against SITL |
| Real-time visualization: Angular 17+ dashboard, charts, topology, 3D, logs, mission/safety panels | WS-VIZ | Production WebSocket gateway (Boost.Beast, C++11-compatible) bridging a reworked TelemetryBus (bounded queues + egress thread); JSON + MessagePack; telemetry recording/playback; full Angular workspace under `dashboard/` |
| Mission-critical safety: multi-layer, hardware/software watchdogs | WS-SAFE | SafetyManager wired to halt the scheduler; software watchdog feeding it; Linux `/dev/watchdog` hardware watchdog integration; measured e-stop latency budget |
| Enterprise security: MFA, RBAC, end-to-end encryption | WS-SEC | TLS (OpenSSL via Conan) on TCP + WebSocket; local user store with TOTP-based MFA; role→permission RBAC enforced on gateway/control API; audit log |
| Cross-platform: Linux, Windows 10+, macOS 10.15+ | WS-PLAT | Winsock + `LoadLibrary` plugin loader + Windows thread-priority path; macOS kqueue-free portable sockets; all three OSes green in CI running the full suite |
| Developer-friendly: docs, tooling | WS-DX | Doxygen API reference, `docs/index.md`, tutorials, verified quick start, CONTRIBUTING/CHANGELOG, conan package |

The foundation phases (0–3 below) are unchanged prerequisites: none of the vision workstreams can sit on a core with C1–C24 unfixed.

**Scoping notes (honest constraints inside full scope):**
- "Distributed systems" v1.0 = multi-node composition via system-to-system port bridging over the hardened transports — not a DDS/consensus layer. That is what the current architecture (`connectToSystem` + adapters) can credibly grow into within v1.0.
- "End-to-end encryption" v1.0 = TLS on every network hop plus authenticated gateway sessions — not application-layer crypto between processing units.
- "<1 µs" is achievable as *timer/measurement* precision and as scheduler-wake p99 on tuned hardware (isolcpus + SCHED_FIFO); the benchmark defines the claim, published per reference platform.
- Hardware watchdog is Linux `/dev/watchdog` in v1.0; other platforms get the software layer.

## 7. Deletion list (lean v1.0)

- `Configuration`: snapshots/rollback, templates, file-watching stubs, `getMemoryUsage`, `getPerformanceMetrics`, `expandKeyPattern` (~350 LOC).
- Logger: entire `axonvex::Log` stream layer (`ColoredStreamLogger`/`FileLogger`, ODR-risky template-char-array design), `logf`/`LOGF_*` until real formatting exists.
- `utils/caching/lruCache.hpp` (unused; `capacity==0` UB); `coreUtilities.hpp` ResourceUtils/DebugUtils/AlignmentUtils/MathUtils/ScopedTimer (std reinventions; keep MemoryOrdering + one `nextPowerOf2`).
- `Path`: std-forwarding boilerplate, `getSystemInfo`, validator registry, backup helpers.
- `WebSocketServer` placeholder (hard compile-gate it now; WS-VIZ replaces it with the real Boost.Beast implementation — the placeholder must never silently satisfy `ProtocolInterface` in the meantime).
- Statistics: collapse `MemoryStatistics` vs `MemoryPoolStatistics` duplication; de-CRTP the base or drop it.
- `system.hpp`: every declared-unimplemented method (C6). **Done** — deleted.

## 8. Phased plan (full-vision v1.0)

Foundation first (Phases 0–3), then the vision workstreams in dependency order. Milestones: **v1.0.0-alpha** after Phase 3, **-beta** after Phase 7, **-rc** after Phase 9.

### Phase 0 — Truth alignment (1–2 days)
- README "Key Features" gains an honest status column (shipped / in development for v1.0) mapped to the WS table in §6; delete fictional community URLs and professional-services copy; real CI badge; fix or rewrite the bridge-app README; add `CONTRIBUTING.md`, `CHANGELOG.md`. SRS §2.3 stays — it is now committed v1.0 scope (WS-VIZ).
- Exit: every claim maps to code, a benchmark, or a tracked v1.0 workstream.

### Phase 1 — Correctness (2–3 weeks)
- Fix C1–C24 in priority order; each fix lands with a regression test (TDD where feasible).
- ~~Add ASan/UBSan/TSan CMake presets + CI jobs (TSan is the gate for C3/C5/C12/C18/C22-class bugs).~~ **Done**: `-DAXONVEX_SANITIZER=address|thread|undefined` (root `CMakeLists.txt`) plus a 3-way `sanitizers` matrix job in CI. All three are green at 375/375; the quarantined tests are listed with reasons in `.github/workflows/ci.yml` — throughput asserts that cannot survive sanitizer slowdown, plus one known race (C24). C29's entry was removed when the pooled port path was deleted. Presets were skipped deliberately: Conan 2 generates `CMakeUserPresets.json`, and a cache option composes with the existing CI flow instead of fighting it.
- Replace sleep-based test assertions with condition-based waits (flake elimination).
- Exit: TSan/ASan/UBSan clean across the full suite; no declared-unimplemented API.

### Phase 2 — Architecture (2–3 weeks)
- Decompose `AxonVexSystem` (LifecycleController / UnitRegistry / SystemPortRegistry / EventBus / HealthMonitor). **Step 1 of 6 done** (2026-08-07): `SystemPortRegistry` extracted, carrying the C9 dual-mutex protocol verbatim; façade API unchanged. Remaining in order: UnitRegistry → `detail::WorkerThread` → EventBus → HealthMonitor → LifecycleController last. Design + invariant-ownership map: `docs/superpowers/specs/2026-08-06-phase2-core-decomposition-design.md` (untracked). **Watch item for the EventBus/LifecycleController steps:** a 6×6 parallel-load trial hung once in `AxonVexSystemTest.ReinitializeAfterCallbackInitiatedEmergencyShutdown`; a pristine-baseline rerun and a second changed rerun were both clean, so it reads as a rare pre-existing lifecycle-teardown flake rather than a regression — but it was not reproduced a third time, so it is an open question, not a closed one.
- Enforce the RT-path invariant; rework scheduler execution model (ready-set per tick, execution outside locks, optional worker pool).
- Wire SafetyManager → scheduler halt path; fix core↔safety layering via a core-owned hook interface.
- ~~Move sockets to compiled `axonvex_net`~~ **Done** (2026-08-07, wave 1): TCP/UDP + the shared address resolver now compile into a real `axonvex_net` shared library; include paths moved to `axonvex_net/{tcp,udp}`. The transport backlog carried out of C11 closed with it — dead-connection lifecycle (no more half-open transports), mid-frame send poisoning, ephemeral-port test fixture, and immediate magic-byte validation. The review of that work also found and fixed two pre-existing shipped defects: `setErrorCallback()` self-deadlocked on every first call (C46), and `UdpSocket::start()` returned `true` while the receive path stayed permanently dead (C47).
- ~~Execute the §7 deletion list~~ **Done** (2026-08-07, wave 1): all seven areas, ~1,620 lines net removed, behavior-preserving. `logf`/`LOGF_*` were **kept** — §7 listed them deletable only "until real formatting exists" and C14 delivered that. WebSocket placeholder is now an `#error` unless explicitly opted into.
- Make `ProcessingUnit` non-movable (**done**, C8); `MissionPipeline` thread-correct (**done**, C22).
- ~~Rework `TelemetryBus`~~ **Done** (2026-08-07, wave 1): bounded non-blocking `publish()` over the house `ThreadSafeQueue` with drop-newest and a `droppedCount()`, one bus-owned egress thread dispatching subscribers via snapshot-then-dispatch, and a stable `{channel, seq, timestamp, payload}` envelope with a v1 JSON encoding and a documented MessagePack slot-in path. Open wire-format question before Phase 9 freezes it: `seq` is assigned at egress and is gapless by construction, so a consumer cannot detect the drops the design deliberately produces — consider a publish-side per-channel sequence or per-channel drop counters.
- **Composer foundations (see §11)** — **partly done** (2026-08-07): `UnitFactory`, the unit introspection/metadata contract, and SystemSpec v1 parsing + validation have landed (decisions: `nlohmann::json` is the parameter currency and is accepted in the plugin-facing signature; port data-type identity is by canonical dot-namespaced name with registration-time collision refusal, no layout hash in v1; `missionPipelines` is reserved-but-rejected until Phase 7). Still to land: `loadFromSpec()` and the `SpecSystem` subclass (a final subclass plus a free `loadSystemFromSpec()`, `initializeBlocksLayout()` stays pure virtual — decided 2026-08-07), plus `BasePort::tryConnect` for type-erased wiring. Original scope statement: define **SystemSpec v1** — a JSON schema describing a full system declaratively (units, port wiring, timing constraints, adapters, safety policies, mission pipelines); add a `UnitFactory` registry (unit types constructible by name) and a unit **metadata/introspection contract** (each unit type describes its ports, data types, and configurable parameters). Implement `AxonVexSystem::loadFromSpec()` so a system can be instantiated from data instead of C++ subclassing — the decomposed registries (UnitRegistry/SystemPortRegistry) are designed around this from day one.
- Exit: `graphify update .` shows the god node dissolved; RT path audited lock/alloc-free; e-stop halts a running system in a test; TelemetryBus decoupled from RT threads; an example system boots from a `.axv.json` spec with zero subclass code.

### Phase 3 — Proof & release engineering (2 weeks) → **v1.0.0-alpha**
- Benchmark harness (Google Benchmark): scheduler jitter, port throughput, e2e latency, e-stop latency; results in `docs/benchmarks.md`; CI perf-regression gate on Linux.
- Real integration tests: TCP/UDP loopback data exchange, real `dlopen` of a built sample plugin `.so`, `RealTimeScheduler` direct tests (privilege-guarded with `GTEST_SKIP`), TelemetryBus tests.
- Packaging: remove `CMAKE_INSTALL_PREFIX` force-override; SOVERSION + symbol visibility; `axonvex/version.hpp`; `conanfile.py` + test_package; CI job that installs and builds a downstream `find_package(axonvex)` consumer.
- Real `.clang-tidy` config; coverage reporting.
- Exit: `conan create` and `find_package` work from a clean machine; benchmarks published.

### Phase 4 — WS-PLAT + transports hardening (3–4 weeks)
- `axonvex_net` production pass: length-prefixed framing, partial-send loops, `MSG_NOSIGNAL`, DNS resolution, IPv6-capable API, fd lifecycle correctness.
- Windows port: Winsock2 sockets, `LoadLibrary`/`GetProcAddress` plugin loader, `SetThreadPriority`/timeBeginPeriod timing path; macOS port verification; remove all silent no-op platform fallbacks (unsupported = compile error or explicit runtime error, never fake success).
- CI: full test suite green on ubuntu/windows/macos, Debug+Release; artifact upload.
- Exit: three-OS CI truthfully green; no simulated-success code paths remain.

### Phase 5 — WS-PERF + WS-SCALE (3–4 weeks)
- Scheduler performance work to the vision numbers: per-tick ready-set execution, optional multi-worker executor, lock-free task stats; documented RT deployment profile (SCHED_FIFO, mlockall, isolcpus, IRQ affinity).
- Port hot path: alloc-free steady state (fixed pools, no string building), benchmark-driven optimization to 1M+ msgs/sec on reference hardware.
- 10,000-block soak test (nightly CI) with memory/jitter regression tracking.
- Multi-node composition: system-to-system port bridging over `axonvex_net` (typed serialization at the boundary), demo app with two processes exchanging pipeline data.
- Exit: published benchmarks meet or honestly restate the README numbers; 10k soak green; two-node demo in CI.

### Phase 6 — WS-SAFE completeness (2 weeks)
- Hardware watchdog: Linux `/dev/watchdog` integration behind the existing `Watchdog` API; petting wired to scheduler liveness; failure → hardware reset path documented.
- Layered safety per the architecture doc: unit-level policies → system SafetyManager → hardware watchdog; latching + hysteresis in the policy model; e-stop latency budget measured end-to-end.
- Exit: pull-the-thread test on real hardware (policy violation → scheduler halt → watchdog pet stops) documented with timings.

### Phase 7 — WS-ECO adapters & plugins (3 weeks) → **v1.0.0-beta**
- ROS 2 plugin complete: default casters for common types implemented, functional `createServer`, registry keyed by (InternalType, RosMsg) pair, `unique_ptr` returns; built as a real MODULE `.so` with ABI handshake; bridge app matches its README.
- MAVLink adapter: `AdapterBase` implementation over UDP using the MAVLink C library, validated against ArduPilot/PX4 SITL in CI (containerized).
- Plugin developer guide + sample plugin template.
- **Composer foundations**: plugins export their unit types into the `UnitFactory` with full introspection metadata (ports, types, parameters), so spec-loaded and future visually-composed systems can use plugin-provided units identically to built-ins.
- Exit: both adapters demonstrably exchange live data in CI; plugin loads via real dlopen/LoadLibrary on both OSes; a plugin-provided unit instantiates from a SystemSpec.

### Phase 8 — WS-SEC (3–4 weeks)
- TLS everywhere: OpenSSL via Conan; `axonvex_net` TLS variants for TCP client/server and the WebSocket gateway; cert config surface in `Configuration`.
- AuthN: local user store (argon2id-hashed credentials) + TOTP MFA for gateway/dashboard sessions; token-based session management.
- AuthZ: role → permission model (viewer / operator / admin) enforced on every gateway control endpoint (mission control, safety override, config mutation).
- Audit log of authenticated control actions through the existing Logger.
- Exit: security test suite (authn/authz/TLS handshake, negative cases); no plaintext control path remains enabled by default.

### Phase 9 — WS-VIZ visualization platform (6–8 weeks) → **v1.0.0-rc**
- Production WebSocket server (Boost.Beast, C++11-compatible with the C++14 tree) replacing the placeholder; TelemetryBus→WS gateway with per-client bounded queues and drop policy; JSON + MessagePack encodings; channel subscribe protocol.
- Telemetry recording (channel snapshots to disk) and playback through the same gateway — reuses the trace/replay design.
- Angular 17+ workspace under `dashboard/` (standalone components, signals): live telemetry charts (ngx-charts/D3), system topology view fed by the unit/port registries, Three.js 3D scene (poses, trajectories), log viewer, mission control panel, safety panel (e-stop, policy states), drag-and-drop layout with session persistence; auth against Phase 8 (login, MFA, role-gated controls).
- **Composer foundations**: the topology view renders from the same **SystemSpec graph schema** the composer will edit (one shared graph model/component, read-only in v1.0); the gateway control API includes `spec/validate`, `spec/get` (export running system as SystemSpec), and RBAC-gated `spec/deploy` (instantiate + start from an uploaded spec) — the composer backend, shipped and tested before its UI exists.
- Dashboard e2e tests (Playwright) against a live example system; dashboard build integrated into CI.
- Exit: `docker compose up` (or equivalent) starts an example system + gateway + dashboard; an operator can watch telemetry, replay a recording, and trigger a role-gated e-stop from the browser.

### Phase 10 — Hardening & release (2 weeks) → **v1.0.0**
- Doxygen API reference + `docs/index.md` + tutorials (first pipeline, first adapter, first plugin, dashboard setup); quick start verified in a clean container.
- Extended soak (24h+), RC bug-fix cycle, release notes, semver/ABI policy documented, tagged release with conan package + dashboard artifacts.

**Totals:** ~30–38 engineering weeks (≈7–9 months solo). The dashboard track (Phase 9 Angular work) is independent of Phases 4–8 once the Phase 2 TelemetryBus contract and Phase 8 auth API are fixed, so a second developer compresses the calendar to ≈4–5 months. Phases 0–1 remain non-negotiable before anything public ships, including an alpha.

## 9. Quality gates for v1.0 (definition of done)

1. TSan/ASan/UBSan-clean full test suite on Linux CI.
2. E-stop test: running pipeline halts within a measured bound after policy violation — including the hardware-watchdog layer on a reference device.
3. Published benchmark numbers backing every README performance claim (<1 µs-class jitter profile, 1M+ msgs/sec, 10k-block soak), regression-gated in CI.
4. Fresh-machine `conan create` + downstream `find_package` consumer build in CI.
5. Real plugin `.so`/DLL loaded via dlopen/LoadLibrary in CI with ABI handshake, on Linux and Windows.
6. ROS 2 and MAVLink adapters exchanging live data in CI (SITL for MAVLink).
7. Full suite green on ubuntu/windows/macos with no simulated-success fallbacks.
8. Dashboard e2e: login with MFA → live charts → replay a recording → role-gated e-stop, automated in CI.
9. No plaintext, unauthenticated control path enabled by default; security test suite green.
10. Zero declared-but-unimplemented public symbols (link test); every README claim traceable to code, test, or benchmark.

## 10. Risks (name them now, not at month six)

- **Solo bandwidth vs. scope**: the vision is a platform, not a library. The milestone gates exist so an alpha/beta is always shippable if the calendar slips; the dashboard track is the natural place to add a second pair of hands.
- **RT numbers are hardware-truths**: <1 µs-class jitter requires tuned Linux (isolcpus, SCHED_FIFO). The benchmark + deployment profile *is* the claim; without dedicated reference hardware the number cannot be honestly published.
- **C++14 ceiling**: Boost.Beast, OpenSSL, and MAVLink C are all C++14-compatible, so no conflict today — but the constraint should be revisited at v1.0 (C++17 unlocks `std::optional`/`std::filesystem` and deletes two compat layers).
- **Security scope creep**: MFA/RBAC is bounded to the gateway/control surface. Anything beyond (LDAP/SSO, per-unit crypto) is explicitly out until v1.x.
- **Windows RT semantics**: Windows cannot match SCHED_FIFO determinism; v1.0 documents per-platform timing classes rather than pretending parity.
- **Composer-foundation drift**: if SystemSpec/UnitFactory/introspection are treated as optional extras during Phases 2–9, the composer (§11) becomes a rewrite instead of a UI. The exit criteria in Phases 2, 7, and 9 exist precisely to prevent this.

## 11. Next release (v1.1) headline — the Visual System Composer

**The product idea:** don't just *observe* an `AxonVexSystem` in the browser — **design and compose one there**. A visual, node-graph web studio where an engineer drags processing units from a palette (built-ins + plugin-provided), wires typed ports on a canvas, attaches timing constraints, safety policies, adapters, and mission pipelines, validates the design, and deploys it to a live runtime — with the v1.0 dashboard then observing what was just composed. Design → deploy → observe → iterate, all in one app. This is the feature that turns AxonVex from a C++ library into a platform.

**Why it works without a rewrite:** v1.0 deliberately ships the entire backend for it —
1. **SystemSpec v1** (Phase 2): the declarative JSON system description *is* the composer's document format.
2. **UnitFactory + introspection metadata** (Phases 2, 7): the palette, property inspectors, and port-type compatibility rules are generated from unit metadata — including plugin units — not hand-maintained.
3. **Gateway spec API** (Phase 9): `spec/validate`, `spec/get`, `spec/deploy` (RBAC-gated) — the composer UI is a client of an API that v1.0 already tests.
4. **Shared graph model** (Phase 9): the dashboard topology view and the composer canvas are one component in two modes (read-only vs. editable).
5. **Security** (Phase 8): composing/deploying is an `admin`-role capability from day one; MFA and audit logging already cover it.

**v1.1 scope (the UI + the deltas):**
- Composer canvas: node-graph editor on the shared graph model (custom Angular canvas or a library such as Rete.js/JointJS — decide by spike); unit palette from `UnitFactory` metadata; typed-port connection validation live on the canvas (type mismatch = can't wire); property inspector per unit/port/connection (parameters, timing constraints, downsampling, memory-pool sizing).
- System-level design surfaces: safety-policy attachment panel, mission-pipeline sub-editor (elements + transition graph), adapter/transport configuration (ROS 2 topics, MAVLink endpoints, TCP/UDP), multi-system composition (bridged systems on one canvas).
- Design lifecycle: save/load SystemSpec documents (git-friendly JSON), spec versioning + diff view, server-side validation with inline errors, dry-run instantiate, deploy-to-runtime, and **round-trip**: import a running system (`spec/get`) back onto the canvas.
- Simulation aid: instantiate the composed system with generator/mock units in a sandbox runtime and watch it in the dashboard before deploying against real hardware.
- Runtime deltas (C++): hot spec re-deploy (stop → swap → start with state disposition rules), richer validation diagnostics (cycle detection, unconnected required ports, timing feasibility check against declared constraints).

**Estimate:** 10–14 weeks after v1.0 (canvas editor ≈ half of it). Frontend-heavy, so it parallelizes cleanly with post-1.0 C++ maintenance.

**Non-negotiable v1.0 exit criteria that guard this feature** (already embedded above): a system boots from `.axv.json` with zero subclass code (Phase 2); plugin units instantiate from spec (Phase 7); `spec/validate|get|deploy` live behind RBAC (Phase 9); topology view renders from the SystemSpec schema (Phase 9).
